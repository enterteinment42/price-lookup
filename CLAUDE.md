# CLAUDE.md — price-lookup

Инструкция для Claude Code при работе с этим репозиторием.

## Текущий фокус

**Статус:** ✅ Сессия 33 (2026-07-23): price-lookup встроен в **обычный поиск магазина** как бесшовный фолбэк — когда каталог по запросу пуст (≥2 симв., дебаунс 350 мс), под «Ничего не найдено» появляется кнопка **«🔍 Игры нет в каталоге? Жми сюда»**; по клику открывается существующая модалка `plm*` с уже подставленным запросом и сама запускает поиск. Для клиента это один поиск — он не знает про отдельный сервис. Реализовано **на стороне магазина** (`js/client.js`, репо poigraem, коммит `173e9e9`): помощник `_schedulePlCta(q)`/`_plCtaOpen()`, контейнер `#cli-pl-cta` в пустой ветке `renderClientList`, вход через уже существовавший `openPriceModal(prefillName)` — внутренности модалки не трогали (паритет копий, PL-24). **В самом price-lookup правок нет** — эндпоинты не тронуты. Ключевое решение по стоимости: дорогой `/api/price-lookup` (Sony, лимит 10/час, риск бана) уходит **строго по клику**, не по ходу набора; на показе кнопки запросов нет. Работает на живом сайте (подтвердил Денис). Нижний standalone-блок «Не нашли игру?» пока оставлен — дублируется осознанно, судьбу решим позже. Открыто: вопросы по **дизайну** кнопки/выдачи Денис решит через claude design. Продуктовое решение (сессия 32) в силе: **путь Б** — «модуль спроса и цен» (см. п. 9). Хвост без движения: **PL-24** (визуальная приёмка на iPhone), PL-16/PL-17 (клик-тест), PL-25/26/4, мелочи.

**Ближайшие задачи по приоритету:**
1. 🚚 **PL-24 — визуальная приёмка на iPhone** (код уже подтверждён живым деплоем сессией магазина): подписка из модалки магазина открывает бота; при лимите/ошибке — сообщение, а не форма «не нашли»; баннер «В цене возможна ошибка»
2. 👁 **Визуальная приёмка кликами на живом сайте** (то, что Claude не может проверить без браузера/устройства): реальный клик «Следить за скидкой» → попап не блокируется на iPhone (PL-16); переключение изданий не сбрасывает «оформлено» при живом клике (PL-17); как выглядит баннер «В цене возможна ошибка» (фича `05b5bf8`); поиск из магазина не сжигает лимит вдвое (PL-15/PL-3 на практике, не только curl)
3. 🔴 **Следующие фиксы:** PL-25 (`tg://` → `https://t.me` в buyButtons, одна строка), PL-26 (дедуп TG-алерта `priceUncertain`), PL-4 (таймаут fetch к Sony)
4. 🟡 **Остаток мелочей:** PL-9 (base ниже первого тира), PL-10 + `Rs.` (хрупкость парсеров цен), PL-12 (`e.message` клиенту), PL-18 (demand без сортировки), PL-20 (`'null'` в CORS), PL-21 (экранирование `h()`/`a()`), PL-22 (guard в `refine()`), PL-27…PL-30 (админ-счётчик, мёртвый CSS, префикс путей, разнобой синих)
5. ⚠️ Проверить в Supabase существование таблицы `price_lookup_log` — если нет, создать (без неё admin.html пуст даже с фиксом PL-1)
6. 💡 Детектор поломки парсера — TG-алерт если все запросы за час `not_found` (PL-7); негативный кэш `not_found` 30–60 мин; откалибровать пороги `TRUST_CONF`/`TRUST_GREY` (PL-31) на реальном трафике после недели работы; последить за долей игр, уехавших в Индию после снижения порога `TR_PRIORITY_FACTOR` до 12% (PL-33) — не задевает ли лишнего
7. 🔴 #2 Срок скидки — «−60% до 24 июня» (данные уже в `__NEXT_DATA__`); #11 Поиск прямо в TG-боте
8. 🚀 **Идеи развития:** история цен + бейдж «лучшая цена за 3 месяца» (таблица `price_history` параллельно кэшу); целевая цена в слежке; SEO-страницы `/game/slug` из `price_lookup_log`; «популярно сейчас» из логов
9. 🧭 **Продукт (решение сессии 32): путь Б.** Price-lookup на продажу — НЕ как «универсальный парсер цен конкурентов» (поддержка парсера на каждый сайт не окупается, рынок занят, юр. риски), а как «модуль спроса и цен» конструктора магазинов: виджет «Не нашли товар? — рассчитаем» + заявки + слежка за ценой + лог незакрытого спроса, источник цены — плагин (PS Store TR/IN — нишевый провайдер игровой вертикали; для других ниш — прайс поставщика CSV/XML/Sheets, ручная цена). Порядок вывода продуктов: чат-бот → avito-helper/content-creator → этот модуль. Задел в коде: мультитенантность, PL-29 (относительные пути), PL-30 (цвета из конфига), абстракция провайдера вокруг `lookupPrice`, надёжность как SLA-фича (PL-7, `store_unavailable`, негативный кэш — обязательны до продажи)

## 🛠 Как проводить аудит

Для полного аудита сервиса (сплошное чтение `server.js`/`parsers.js`/`public/*.html` + сверка с виджетом магазина `js/client.js`, персоны клиентов, проверка точности парсинга/просчёта цены/UX/архитектуры, перепроверка старых PL-N без дублей, сквозная нумерация новых находок, приоритизированный отчёт без правок кода) — использовать skill **`/price-lookup-audit`**, файл `D:\My Apps\price-lookup\.claude\skills\price-lookup-audit\SKILL.md`. Skill локальный, в git сознательно НЕ коммитится (аудиты Денис проводит только на этой машине, VPS — только деплой). Составлен по итогам расширенного аудита Fable 2026-07-13 (PL-23…PL-30).

## 🔍 Аудит Fable (2026-07-02, сессия 25) — нумерация PL-N

Полное чтение `server.js`, `parsers.js`, `public/index.html`, `public/admin.html`. Пять багов сессии 24 (см. «Известные мелкие баги») не дублируются. Ничего НЕ исправлено.

**Критичные:**

- **PL-1. admin.html — неправильный путь API.** `public/admin.html:82`: `fetch('/api/admin/logs?token=...')` без префикса `/price-lookup`. Страница открывается по `api.poigraem.shop/price-lookup/admin.html`, а запрос уходит на `api.poigraem.shop/api/admin/logs` — это ОСНОВНОЙ server.js магазина, роута там нет → 404 → «❌ Ошибка». Тот же класс бага, что чинился в сессии 16 для `/api/config` в index.html. Фикс: `/price-lookup/api/admin/logs`. Пока не исправлено — создание таблицы `price_lookup_log` админку не оживит.
- **PL-2. `refine()` не обрабатывает 429/ошибки.** `public/index.html:376–393`: в `doSearch()` ветка `if (r.status === 429)` есть, в `refine()` (клик по кандидату в «Вы имели в виду?») — нет. При 429/500 ответ `{error, message}` без `status` → `render()` уходит в else → `showForm()` — клиенту ложно показывают «Не нашли эту игру». То же в `doSearch()` для не-429 ошибок (400/500).
- **PL-3. Общий счётчик rate-limit на поиск + заявку + подписку.** `server.js:587–590`: один экземпляр `limiter` (10/час) смонтирован на `/api/price-lookup`, `/api/price-request` и `/api/price-watch` — у express-rate-limit общий инстанс = общий счётчик по IP. Потеря лида: клиент поискал 10 игр → заполняет форму «рассчитаем вручную» → 429 → заявка не уходит. Заявка/подписка не дёргают Sony — им нужен отдельный лимитер (как сделали для `/api/suggest`).

**Существенные:**

- **PL-4. `fetchHtml` без таймаута.** `server.js:147–158`: fetch к Sony без AbortController. Для IGDB таймауты есть (Promise.race 2000/2500 мс), для Sony — нет. Зависшее соединение (Cloudflare-челлендж) держит «Ищем…» до дефолтного таймаута undici (сотни секунд); блокирует и `checkWatchlist`.
- **PL-5. Сырой запрос вместо названия игры.** `public/index.html:320`: при одном издании `buyButtons(gameName, ...)` — `gameName` это ввод клиента («киберпанк»), а не `d.neutralLabel` («Cyberpunk 2077»). В `?order=` магазина и сообщение менеджеру уходит сырой запрос. Мульти-издательская ветка (строка 371) корректно использует `ed.name`. Фикс: `d.neutralLabel`.
- **PL-6. `checkWatchlist` бомбит Sony без пауз.** `server.js:861–891`: цикл по подпискам зовёт `lookupPrice` подряд без задержки. Интервал 6ч = TTL кэша → к моменту проверки кэш истёк → каждая подписка = 2 живых запроса TR+IN без промежутка. Нарушает собственное правило 200–800 мс; риск бана растёт с числом подписок.
- **PL-7. Контракт `store_unavailable` не реализован.** CLAUDE.md обещает `{ status: "store_unavailable", managerLink }` при недоступности Sony. В коде статуса нет: оба региона упали + нет stale-кэша → `[]` → честный `not_found`. Клиент видит «не нашли» про существующую игру, лог пишет not_found → за время сбоя Sony статистика «незакрытого спроса» засоряется. Связан с идеей «детектор поломки парсера».

**Мелочи:**

- **PL-8.** `public/index.html:509–510` — deep-link `?game=X` запускает `doSearch()` не дожидаясь `fetch('/api/config')`. Гонка: кнопка «👀 Следить» не отрисуется (`cfg.botName` пуст), TG-кнопка уйдёт на дефолтный handle `poigraem_shop`. Это основной сценарий трафика из чат-бота.
- **PL-9.** `server.js:171` — `convertToRub`: если `base` < `markupTiers[0].from`, ни один тир не совпадёт → применится наценка ПОСЛЕДНЕГО тира (логичнее первого). Сейчас тиры с 0 — мина спит.
- **PL-10.** `parsers.js:82` — TRY-регэксп `\d{1,3}(?:\.\d{3})*` на цене без разделителя тысяч («1145,00 TL») из-за бэктрекинга схватит «145,00» → 145 вместо 1145. Sony сейчас всегда ставит точки; при смене формата парсер начнёт тихо занижать цены.
- **PL-11.** `parsers.js:306, 360–363` — проверки `startsWith(queryNorm + ':')` и `:` в регэкспе `isWordInMiddle` мертвы: `normalizeQuery` удаляет пунктуацию, двоеточий не бывает. Безвредно.
- **PL-12.** `server.js:745` — `/api/price-lookup` при 500 отдаёт `e.message` клиенту — может светить URL Sony с регионом (против правила «не раскрывать источник»).
- **PL-13.** `public/index.html:229` — Escape закрывает модалку только при фокусе в поле поиска (обработчик на `qInput`, не на `document`).
- **PL-14.** `public/index.html:361–373` — в мульти-издательском режиме верхняя обложка берётся только из `ed.coverUrl`; серверный `d.coverUrl` (IGDB-фоллбэк) игнорируется — если у изданий обложек нет, наверху 🎮-плейсхолдер, хотя фоллбэк был получен.

**Известные мелкие баги (зафиксированы, не критичны):**
- `sendRequest()` (`public/index.html`) не передаёт `source` → все заявки с формы пишутся как `'direct'` даже при переходе из чат-бота
- `checkWatchlist` (`server.js`): двойное уведомление возможно если `sendChatbotMessage` успел, а `update(active:false)` упал по сети
- `NON_GAME_CLASSIFICATIONS` (`parsers.js`) — определён, но нигде не используется (мёртвый код)
- `watchGame()` (`public/index.html`) — не проверяет `r.ok` перед `r.json()`: при 429/500 кнопка молча сбрасывается без сообщения пользователю
- CSS `.m-ed-btn` (`public/index.html` строка ~135) — стиль остался после удаления кнопок в сессии 23

**Факты инфры (актуально на сессию 22):**
- **Обложка PS Store** лежит в `item.media` распарсенной страницы поиска — массив `{role, type, url}`. Роли: `PREVIEW`(видео), `EDITION_KEY_ART`, `FOUR_BY_THREE_BANNER`, `GAMEHUB_COVER_ART`, `LOGO`, `PORTRAIT_BANNER`, `MASTER`. Хелпер `pickCover` (`parsers.js`) берёт по приоритету `PORTRAIT_BANNER → MASTER → GAMEHUB_COVER_ART → первая IMAGE`. Сервер: PS-обложка с фоллбэком на IGDB. **0 лишних запросов** (данные уже скачаны), кэшируется в составе `result`.
- **`LATIN_ALIASES`** (`server.js`) — латинские синонимы франшиз по годам (`fc 26 → ea sports fc 26` и т.д.). Нужны, т.к. матчер режет короткий запрос (2 слова против 8 в имени бандла) по порогу покрытия 40%. **Обновлять по годам** при выходе FC 27 и т.п. — добавить строку в список (как и кириллические `фк`/`фифа` в `CYRILLIC_TRANSLATE`).
- **`/api/suggest` на отдельном лимите** `suggestLimiter` (120/час, `SUGGEST_LIMIT_PER_HOUR`) — автоподсказки бьют в IGDB (не Sony) и дёргаются на каждую букву, на строгом лимите `/api/price-lookup` (10/час) они выбивали бюджет поиска за 2-3 запроса.
- **Деплой (обновлено сессией 31):** репо теперь отдельный — `raw.githubusercontent.com/enterteinment42/price-lookup/main/<файл>` (корень репо, без `Standalone/`). Сервис на VPS живёт в `~/price-lookup` — **обязательно `cd ~/price-lookup` перед curl**: 2026-07-13 деплой без cd ушёл в `~`, сервис не обновился; сигналом был огромный `diff` живого файла против скачанного. Шаблон: `cd ~/price-lookup` → `curl -L -o файл.new <url>` → `grep -c <маркер> файл.new` → `cp файл.new /tmp/chk.mjs && node --check /tmp/chk.mjs` → `diff файл файл.new` (только ожидаемые изменения!) → `cp файл файл.bak.$(date +%Y%m%d-%H%M%S)` → `mv файл.new файл` → `grep` по живому файлу → `pm2 restart price-lookup`. ⚠️ Внимательно с `mv` — в сессии 22 дважды `mv` НЕ выполнился (был в одной строке с curl следующего файла), работал старый код. ⚠️ `node --check файл.new` НЕ работает (сессия 31): при `"type": "module"` Node не выводит модульность для расширения `.new` и падает не по делу — проверять синтаксис на копии с `.mjs` (как в шаблоне выше), иначе шаг «синтаксис до подмены» ложно краснеет и его начинают пропускать. `recon/` в `.gitignore` — при потере восстанавливать свежими страницами PS Store (curl с браузерным UA).
- **Кэш маскирует правки парсера.** `getCached`/`setCached` хранят весь массив кандидатов (TTL 6ч). После правки парсера популярные игры (Cyberpunk, Stellar Blade) ещё 6ч отдают старые данные. Для чистого теста — `/test/search/:region/:query?token=ADMIN_TOKEN` (ходит к Sony мимо кэша) или свежая игра.
- Таблица `price_watchlist` создана **без RLS**, но потребовала ручной `GRANT SELECT,INSERT,UPDATE … TO anon, authenticated, service_role` + грант на sequence `price_watchlist_id_seq`. Без гранта `/init` падал с `permission denied for table`.
- `tg-bot.js` чатбота использует `SUPABASE_SERVICE_KEY` (есть в `~/poigraem-chatbot/.env`). На Node 20 supabase-realtime требует `import ws from 'ws'` + `{ realtime: { transport: ws } }`.
- Рейт-лимит price-lookup (10/час, in-memory) обнуляется при `pm2 restart price-lookup` — удобно для тестов.
- Deep-link: `t.me/Poigraem_Game_Helper_bot?start=w_<token>`. Обработчик: `Standalone/chat-bot/tg-bot.js` (`handleMessage`, ветка `/start w_`).

## 🔍 Аудит Fable (2026-07-07, сессия 26) — нумерация PL-15…PL-22

Третий аудит, самой мощной моделью (Fable 5). Сплошное чтение `server.js`, `parsers.js`, `public/index.html`, `public/admin.html` + сверка с точками вызова price-lookup из основного репо магазина (`js/client.js`, `js/admin-images.js`). Все 14 находок сессии 25 (PL-1…PL-14) перепроверены по актуальному коду — подтверждены, ничего не исправлено. **PL-19 закрыт в сессии 28** (код+грант в Supabase+живое подтверждение в логах) — детали в `AUDITS-ARCHIVE.md`. Остальное из PL-15…PL-22 ещё НЕ исправлено.

**Критичные:**

- **PL-15. CORS-preflight сжигает лимит поиска и ломается сам.** `server.js`: лимитеры (`app.use('/api/price-lookup', limiter)` и соседние, строки 587–591) смонтированы ДО CORS-обработчика (строка 594). Виджет магазина зовёт сервис с другого домена (`poigraem.shop` → `api.poigraem.shop`, см. `js/client.js:1703`), а POST с JSON всегда шлёт предварительный OPTIONS-запрос. Итог: каждый поиск из магазина тратит 2 из 10 токенов лимита (OPTIONS + сам POST) — реальный бюджет клиента не 10, а 5 поисков в час, причём общий с заявкой и подпиской (PL-3). Хуже: когда лимит кончился, 429 получает сам OPTIONS-запрос — браузер без CORS-заголовков в ответе видит сетевую ошибку, клиенту показывается «Проверьте соединение» вместо честного «попробуйте через час». У `coversLimiter` защита уже есть (`skip: (req) => req.method === 'OPTIONS'`, строка 585) — у `limiter` и `suggestLimiter` нет. Фикс: переставить блок CORS-middleware выше `app.use('/api/...', limiter)` — тогда OPTIONS отвечается 204 до счётчика лимита.

**Существенные:**

- **PL-16. «Следить за скидкой» глохнет на iPhone.** `watchGame()` (`public/index.html:466–488`) делает `window.open()` ПОСЛЕ `await fetch(...)` — Safari на iOS блокирует всплывающие окна, открытые не синхронно в обработчике клика. Подписка в Supabase создаётся, а бот не открывается → `tg_chat_id` не привязывается (обработчик `/start w_TOKEN` в chatbot его и не увидит) → уведомление о скидке никогда не придёт. Фикс: открыть пустое окно (`window.open('', '_blank')`) синхронно в момент клика, после ответа сервера подставить в него URL через `.location`. ⚠️ Исправлено в коде и задеплоено (сессия 28, коммит `0eafa89`), визуальная приёмка на iPhone ещё не проведена — формально не закрыт.
- **PL-17. Переключение изданий сбрасывает состояние кнопки слежки.** `selectEdition()` (`public/index.html:338–374`) полностью перерисовывает `smodal-body`, включая блок кнопок — после успешного «✅ Открой бот и нажми Start» клик по другому изданию в списке молча возвращает кнопку в исходное «👀 Следить за скидкой». Риск: клиент может создать несколько дублирующих pending-подписок на разные издания одной игры, не заметив, что первая уже была оформлена. ⚠️ Исправлено в коде и задеплоено (сессия 28, коммит `0eafa89`), визуальная приёмка ещё не проведена — формально не закрыт.
- **PL-19 — ✅ закрыт, сессия 28.** Детали — в `AUDITS-ARCHIVE.md` (команда `/session-audits-archive`).

**Мелочи:**

- **PL-18. Демand-запрос в админке без сортировки/окна по датам.** `GET /api/admin/logs` (`server.js:792–818`): запрос `not_found`-строк (`.limit(2000)`, строка 801) не имеет `.order('created_at', ...)`. Пока строк < 2000 — работает верно, но как только `price_lookup_log` наберёт больше 2000 записей со статусом `not_found`, Supabase начнёт отдавать произвольные (не обязательно последние) 2000 строк, и «Незакрытый спрос» в админке будет считать топ по случайной выборке, а не по актуальной истории. Фикс: добавить `.order('created_at', { ascending: false })` и/или фильтр за последние 30–60 дней.
- **PL-20. `'null'` в `ALLOWED_ORIGINS`.** `server.js:593` — строка `'null'` в белом списке CORS origin пропускает запросы из `null`-origin контекстов (sandboxed iframe без `allow-same-origin`, локальный файл с диска через `file://`). Похоже на оставшийся отладочный вход. Для локальной разработки лучше отдельный env-флаг, не постоянная запись в проде.
- **PL-21. Экранирование на фронте неполное.** `h()` и `a()` (`public/index.html:506–507`) — `h()` не экранирует кавычки, `a()` не экранирует `&`. В местах вида `src="${h(coverUrl)}"` или `onclick="watchGame('${a(gameName)}',...)"` (данные приходят из PS Store / ввода клиента) кавычка в названии игры или URL обложки потенциально разрывает атрибут/JS-строку. Пока не эксплуатировалось, но дёшево укрепить: `h()` добавить `.replace(/"/g,'&quot;')`, `a()` добавить `.replace(/&/g,'&amp;')` первым шагом.
- **PL-22. `refine()` без guard на повторный клик.** В отличие от `doSearch()` (`if (document.getElementById('qBtn').disabled) return`, строка 269), `refine()` (строка 376) не проверяет состояние загрузки — быстрые клики по двум карточкам-кандидатам в «Вы имели в виду?» могут вызвать гонку двух параллельных запросов, и модалка отрендерит тот ответ, что придёт последним (не обязательно тот, на который кликнули последним).

**🚀 Идеи развития (что есть у больших — PSprices, GG.deals, DekuDeals):**
- **История цен + бейдж «лучшая цена за 3 месяца»** — вся инфраструктура кэша уже есть, достаточно параллельно писать каждый живой запрос в новую таблицу `price_history`. Превращает «ориентировочную цену» из слабости в экспертность.
- **Срок скидки** (уже в бэклоге как #2) — «−60% до 24 июня», данные уже в `__NEXT_DATA__`, самый дешёвый прирост конверсии.
- **Целевая цена в слежке** — вместо зашитого порога −15% дать клиенту поле «сообщить, когда ниже X ₽» с предзаполнением −20%.
- **SEO-страницы `/game/slug`** — сервис сейчас невидим поисковикам (только виджет). Рендер страницы игры из уже накопленного `price_lookup_log` + sitemap — органический трафик «купить X дёшево ps5», самый большой рычаг роста аудитории до запуска рекламы.
- **«Популярно сейчас»** — счётчик поисков конкретной игры за неделю прямо в результате, данные уже есть в логах, 0 внешних запросов.
- **Поиск в TG-боте** (#11 в бэклоге) — один `fetch` в `tg-bot.js`.
- **Надёжность как страховка вышеперечисленного:** детектор поломки парсера (TG-алерт, если все запросы за час `not_found`) + негативный кэш 30–60 мин + реализация обещанного `store_unavailable` (PL-7) — Sony меняет вёрстку раз в 2–6 месяцев, сейчас об этом узнают только по потерянным клиентам.

(Этот раздел обновляется командой `/session-end`)

## 🔍 Аудит Fable (2026-07-13, расширенный) — нумерация PL-23…PL-30

Четвёртый аудит (Fable 5), первый по методике skill `/price-lookup-audit` (составлен по его итогам). Сплошное чтение `server.js`, `parsers.js`, `public/index.html`, `public/admin.html`, `test-parsers.js` + сверка с виджетом магазина (`poigraem/js/client.js`, блок `plm*`). Правок кода НЕ было — только аудит.

**Подтверждено в коде (сверка деплоя сессии 28):** CORS выше лимитеров (`server.js:597-610`), `leadLimiter` (`server.js:645-648`), различение not_found/ошибки в `render()` (`index.html:344-349`), синхронный `window.open` в `watchGame` (`index.html:490`), пауза в `checkWatchlist` (`server.js:993`), валидация `priceRub` + `cleanupStaleWatches` (`server.js:906-908, 1004`). Все старые открытые находки (PL-4, PL-7…PL-14, PL-18, PL-20…PL-22, мелкие баги сессии 24) перепроверены — подтверждены, не исправлены. Уточнение по багу сессии 24 «watchGame не проверяет r.ok»: краха больше нет (`d.token`-проверка + catch), но отказ молчаливый — при 429/500 кнопка возвращается без сообщения клиенту.

**Критичные:**

- **PL-23 — ✅ закрыт, сессии 29–30.** Детали — в `AUDITS-ARCHIVE.md` (команда `/session-audits-archive`).
- **PL-24. Виджет магазина (главный трафик) не получил фиксы сессии 28 — три расхождения.** Файл `js/client.js` в репо **poigraem** (фикс живёт там): (1) `plmWatch` (`client.js:2018`) делает `window.open` ПОСЛЕ `await` — баг класса PL-16 жив в магазине, подписка с iPhone из виджета глохнет; (2) `plmRender` (`client.js:1925-1927`) — ветка `else → _plmShowForm()`: 400/500/ответ без `status` → ложное «не нашли» (класс PL-2; 429 перехвачен отдельно раньше); (3) баннер `priceUncertain` в `client.js` отсутствует полностью (grep — 0 вхождений) — фича `05b5bf8` работает только на standalone-странице. Корневая причина — модалка существует в двух почти идентичных копиях (`public/index.html` и `client.js` plm*), которые уже разошлись. Минимум — чеклист паритета при каждой правке; правильно — единый источник виджета. ⚠️ **Сессия 30:** все три части подтверждены закоммиченными и задеплоенными — живое чтение `poigraem.shop/js/client.js` показало корректный синхронный `window.open` в `plmWatch`, честную ошибку в `plmRender`, баннер `_plmWarnHtml()` на месте. Формально НЕ закрыт — визуальной/боевой проверки на реальном iPhone не было (у Дениса нет устройства под рукой, у Claude нет браузерного инструмента для клик-теста).

**Существенные:**

- **PL-25. «Написать в Telegram» на мобильных — тупик `tg://resolve` + расхождение лога сессии 28 с кодом.** `public/index.html:464-467`: `buyButtons()` на iPhone/Android строит `tg://resolve?...` — без установленного Telegram это мёртвый клик/ошибка на шаге покупки. Магазин эту граблю прошёл (COM-5: всегда `https://t.me`). ⚠️ Лог сессии 28 утверждает «убран мёртвый tg://resolve на мобильных» — по коду убран только в `watchGame`, в `buyButtons` остался. Фикс — одна строка: всегда `https://t.me`.
- **PL-26. TG-алерт `priceUncertain` без дедупликации — спам при недоступности TR.** `server.js:471-474`: комментарий обещает «один раз шлём алерт», но `sendTgAlert` уходит на КАЖДЫЙ поиск с trUnavailable. Пустые TR-ответы не кэшируются, IN кэшируется → пока Sony-TR блокирует, каждый поиск популярной игры = ретрай + алерт. 30 поисков за вечер = 30 одинаковых сообщений, настоящий сигнал утонет. Фикс: in-memory дедуп по `cacheKey` с TTL ~1 час (по образцу `_settingsCache`).

**Мелочи:**

- **PL-27. Админка: «Поисков: 100» — размер выборки, не счётчик.** `admin.html:99-100` показывает `logs.length` при серверном `.limit(100)` (`server.js:880`) — при >100 логов шапка навсегда «Поисков: 100», статистика выглядит общей, а считается по последней сотне. Фикс: подписать «за последние 100» либо отдавать `count` отдельно.
- **PL-28. Мёртвый CSS:** `.m-alts`, `.m-alts-label`, `.m-alt-item`, `.m-alt-price` (`index.html:119-123`) — остатки старого списка альтернатив, в JS не используются (туда же известный `.m-ed-btn`, `index.html:136`).
- **PL-29. Захардкоженный префикс `/price-lookup/` во всех fetch фронтенда** (`index.html:218,244,277…`) — локальный `npm run dev` на :3001 отдаёт страницу, но все запросы бьют в несуществующие пути. Фикс: относительные `api/...` — работают и локально, и за nginx.
- **PL-30. Разнобой акцентных синих:** `--accent: #3d9dff` (поиск/цена) vs `#2563eb` (кнопка корзины, выбранное издание — `index.html:129,136,145`). Фикс: свести к одному через `var(--accent)`.
- **Заметка к PL-10 (без нового номера):** к хрупкости TRY-регэкспа добавляется INR-сторона — формат `Rs. 499` (с точкой) `parsePriceText` вернёт null → игра молча станет «Unavailable». Обе валюты чинятся одним заходом + кейсы в `test-parsers.js`.

**Приоритет фиксов (обновлено сессией 30, PL-23 закрыт):** 1) визуальная приёмка PL-24 на реальном iPhone (код подтверждён живым) → 2) PL-25 (одна строка) → 3) PL-26 (дедуп алерта) → 4) хвост «воронки» PL-4/9/10+Rs./12 → 5) мелочи PL-27…30 + старые PL-18/20/21/22. Также поправить формулировку в логе сессии 28 (см. PL-25), чтобы следующая сессия не посчитала tg:// убранным.

## Команды для Дениса

### `/session-end`

Когда Денис пишет `/session-end`:
1. Обнови раздел `## Текущий фокус` вверху файла актуальным состоянием
2. Добавь НОВУЮ запись В НАЧАЛО раздела `## Лог сессий` (НЕ редактируй существующие записи!):
   - **Дата** (текущая)
   - **Что сделано** — конкретные изменения в коде/фичах
   - **Что решили** — архитектурные/продуктовые решения этой сессии
   - **Новые баги** (если нашли) — с указанием файла и описанием
   - **Открытые вопросы** — что обсуждали, но не решили
   - **Что осталось** — задачи на следующую сессию
3. Если в логе стало больше 5 записей — самые старые (сверх 5) перенеси в `SESSIONS-ARCHIVE.md` (в начало файла, создай его если нет). В CLAUDE.md всегда держим 5 последних сессий.
4. **Перенос закрытого в `AUDITS-ARCHIVE.md`** (создай файл если нет): пройдись по CLAUDE.md и найди аудиты/находки/задачи (PL-N и любые другие), которые за эту сессию стали ПОЛНОСТЬЮ закрытыми. Перенеси текст в `AUDITS-ARCHIVE.md` с пометкой «✅ закрыт, сессия N, коммит(ы) …», в CLAUDE.md оставь строку-указатель. ⚠️ Не переноси, если закрытие не подтверждено явно (код готов/задеплоен, но не проверено визуально/боевым прогоном — это НЕ закрытие).
5. **Проверка незакоммиченных/незапушенных правок:** выполни `git status --short` и проверку незапушенных коммитов (`git log --branches --not --remotes --oneline`), покажи Денису список изменённых, но не закоммиченных файлов и коммитов, не отправленных на origin, напомни закоммитить/запушить. Не коммить сам без явной просьбы — только напоминание.
6. Спроси: «Что ещё зафиксировать?»

### `/session-start`

Когда Денис пишет `/session-start` или начинает новый чат с упоминанием проекта:
1. Прочитай раздел `## Текущий фокус`
2. Прочитай 2-3 последние записи в `## Лог сессий`
3. Резюмируй: «В прошлый раз остановились на [...]. Открыто [...]. Следующий шаг был [...].»
4. Спроси: «Продолжаем с этого или новое направление?»

### `/session-archive`

Когда Денис пишет `/session-archive`:
1. Прочитай `SESSIONS-ARCHIVE.md` (если файла нет — скажи об этом)
2. Резюмируй ключевые моменты по категориям:
   - **Решения** — что выбрали и почему (архитектура, стек, бизнес-логика)
   - **Баги** — какие находили и как чинили (паттерны повторяющихся проблем)
   - **Паттерны** — что делали часто, что работало хорошо, что нет
3. Спроси: «Что именно ищем?» — и по уточнению найди конкретную запись или период

### `/session-audits-archive`

Когда Денис пишет `/session-audits-archive`:
1. Прочитай файл `AUDITS-ARCHIVE.md` (закрытые аудиты, задачи и факты, вынесенные из CLAUDE.md)
2. Резюмируй ключевые моменты — какие аудиты/задачи закрыты, что было исправлено, важные паттерны
3. Спроси: «Что именно ищем?»

## Что это

Standalone-сервис «Не нашли игру?» для интернет-магазина «Поиграем» (продаёт цифровые игры PS5/PS4). Клиент вводит название отсутствующей в каталоге игры — сервис автоматически парсит цены через PS Store (Турция и Индия), конвертирует в рубли с наценкой магазина и показывает «ориентировочно от X ₽».

**Не часть основного магазина.** Отдельный Node.js сервис на отдельном порту.

Полная бизнес-логика и архитектурные решения — в `BRIEFING.md`. Прочитай его перед существенными изменениями.

## Стек

- **Node.js 18+** (встроенный `fetch`)
- **Express** — HTTP-сервер
- **Cheerio** — парсинг HTML
- **@supabase/supabase-js** — кэш цен, заявки, чтение настроек магазина
- **express-rate-limit** — защита от абуза
- **dotenv** — переменные окружения
- **ES Modules** (`"type": "module"` в package.json) — используем `import`, НЕ `require`

## Команды

```bash
npm install         # установить зависимости
npm run dev         # запуск с автоперезагрузкой (node --watch)
npm start           # production-запуск
```

Сервис слушает порт из `PORT` (по умолчанию 3001).

## Структура файлов

Walking skeleton: на старте 3 файла. По мере роста — разносим.

```
price-lookup/
├── package.json       # ES modules, зависимости
├── server.js          # ВЕСЬ код: парсеры, matcher, эндпоинты
└── .gitignore
```

**Принцип «walking skeleton сначала»:** парсеры, fuzzy matcher и эндпоинты пока живут в одном `server.js`. Разносим по файлам когда что-то реально вырастет (>300 строк или станет мешать читать), не наперёд. Когда понадобится — разнесём так:

```
price-lookup/
├── server.js          # роутинг, эндпоинты
├── parsers/
│   ├── tr-parser.js   # парсинг PS Store Turkey
│   └── in-parser.js   # парсинг PS Store India
├── matcher.js         # fuzzy matching, нормализация запросов
├── cache.js           # CRUD по price_lookup_cache в Supabase
└── ...
```

## Архитектурные решения (нерушимые)

### Текущий статус (актуально)

Парсеры и matcher вынесены в `parsers.js`. Сервис **полностью рабочий**:
- `parseSearchPage(html, region)` — парсит `__NEXT_DATA__` со страницы поиска PS Store
- `parsePriceText(text, currency)` — понимает TL/₺ (Турция) и ₹/Rs (Индия)
- `matchGame(query, candidates, opts)` — fuzzy match с биграммами, TR-приоритет, isEditionOfSameGame
- `normalizeQuery(s)` — lowercase, NFD, римские → арабские (кроме x)
- `lookupPrice(gameName)` — параллельный поиск TR+IN, конвертация в RUB, кэш в Supabase

### Скрейпинг чужого сайта — главная боль

PS Store не имеет публичного API. Используем HTML-скрейпинг.

**Это значит:**
- Sony может поменять вёрстку → парсер сломается. Раз в 2-6 месяцев это случается.
- Cloudflare может заблокировать IP → нужен User-Agent обычного браузера + рандомные задержки
- Юридически серая зона → НЕ говори клиенту «данные из PS Store», формулировка «ориентировочно от»

**Делай:**
- Все запросы к Sony — через `fetchHtml()` с реалистичным User-Agent и `Accept-Language`
- Между запросами рандомная задержка 200-800ms (если параллельно — Promise.all с задержками в каждом)
- Кэш в Supabase на 24 часа ОБЯЗАТЕЛЕН — без него быстро забанят
- При 403/5xx/timeout от Sony — деградация к форме-заявке «рассчитаем вручную»

**Не делай:**
- Не дёргай Sony без кэширования
- Не используй Selenium/Puppeteer без крайней необходимости — медленно и тяжело для VPS
- Не указывай клиенту регион из которого цена (TR/IN) — защита механики ценообразования

### Структурированный поиск

PS Store скорее всего рендерит страницу поиска через Next.js. Это значит данные могут быть:

**Вариант A:** в HTML карточках результатов (`<article>`, `<div data-testid="...">`)
**Вариант B:** в `<script id="__NEXT_DATA__">{...JSON...}</script>` в конце страницы

Сначала пробуем найти `__NEXT_DATA__` — там обычно структурированный JSON. Если нет — парсим карточки. Это надо проверить на реальной странице.

### Двухрегиональная логика

Параллельно дёргаем PSN IN и PSN TR. **Это всегда `Promise.all`**, никогда последовательно (×2 latency без выгоды). Каждый регион в отдельной функции с обработкой ошибок — если один регион упал, второй должен сработать.

### Расчёт цены в RUB

Курс валют и `markupTiers` берутся **из общей Supabase магазина** (читаем настройки). НЕ хардкодь курсы. НЕ дублируй логику ценообразования — используй ту же что у магазина.

Формула (аддитивная наценка из тиров):
```javascript
base = priceLocal * rate          // для TRY
base = ceil(priceLocal/1000) * inrGift  // для INR
tier = markupTiers.find(t => base >= t.from && (t.to == null || base <= t.to))
priceRub = ceil((base + tier.markup) / rounding) * rounding
```

`markupTiers` — массив диапазонов `{from, to, markup}` из настроек магазина. `inrGift` — стоимость 1000 INR в рублях.

### Выбор региона (TR-приоритет) — БИЗНЕС-КРИТИЧНО

Обе цены (TR и IN) конвертируются в рубли, дальше **не** берём просто минимум. Турция — приоритетный регион (обычно дешевле и стабильнее по механике гифт-карт), Индия побеждает, **только если её рублёвая цена дешевле турецкой минимум на 12%** (`server.js`, `TR_PRIORITY_FACTOR = 1 - 0.12`): `inRub <= trRub * TR_PRIORITY_FACTOR`.

- **Процент считается ОТ турецкой (большей) цены** — как и все проценты в проекте (скидки PS Store, `WATCH_PRICE_DROP_THRESHOLD`). Не путать со знаменателем «Турция дороже Индии на X%» — это другое число (13.72% от TR = 15.91% от IN на одном и том же разрыве). Меняешь порог — меняй в терминах «дешевле от TR», иначе правило молча поедет.
- Порог живёт в **одной** константе `TR_PRIORITY_FACTOR` и применяется в двух местах: выбор winner (`server.js:~468`) и дедупликация списка изданий (`server.js:~495`). Менять только через константу — иначе winner и список изданий разъедутся.
- Калибровка (сессия 31): порог опущен 20% → 12% под класс «новых игр» вида **3449 TL / 4999 ₹** (разрыв ~13.7%). Маркерный кейс — Marvel's Wolverine: TR 7360 ₽ vs IN 6350 ₽ → при 12% выигрывает Индия (6350 ₽) с запасом ~127 ₽ на движение курса. При 16–20% выигрывала Турция. Порог глобальный: вместе с этим классом на Индию уходят ВСЕ игры в зоне 12–20%.

### Standalone-изоляция

- НЕ модифицируй основной server.js магазина
- НЕ создавай зависимости от кода магазина
- Связь с магазином ТОЛЬКО через общую Supabase (read для настроек, read/write для своих таблиц)
- Добавление в корзину магазина — позже, через REST API основного server.js или прямую запись в Supabase

## Переменные окружения

Все секреты — только в `.env`, НИКОГДА в коде. `.env` в `.gitignore`.

Полный список с примерами — в `.env.example`. Ключевые:

```
PORT=3001
SUPABASE_URL=https://...supabase.co
SUPABASE_ANON_KEY=...
RATE_LIMIT_PER_HOUR=10
# RATE_LIMIT_PER_DAY и CACHE_TTL_HOURS из env не читаются — TTL захардкожен 6ч в коде
MANAGER_PRIMARY_TYPE=telegram
MANAGER_PRIMARY_VALUE=@your_handle
MANAGER_FALLBACK_TYPE=vk
MANAGER_FALLBACK_VALUE=https://vk.com/...
```

## Таблицы Supabase

Используем общую Supabase магазина. Price-lookup владеет двумя таблицами и пишет в одну общую:

- **`price_lookup_cache`** — кэш найденных цен. Поиск по `query_normalized`, TTL 24 часа. Структура: `query_normalized, region, concept_id, price_local, currency, price_rub, edition_label, found, fetched_at`
- **`price_lookup_requests`** — заявки «не нашли, рассчитайте вручную». Структура: `game_query, client_contact, contact_type, status, ip_hash, source, created_at, processed_at`
- **`event_tracking`** (общая для магазина+price-lookup+chatbot) — события переходов между сервисами

Полная структура — в `BRIEFING.md` раздел «База данных».

## Контракт API

### POST /api/price-lookup
Запрос: `{ gameName, editionHint?, source? }` где `source` это `direct` или `chatbot`
Ответ (успех): `{ status: "found", priceRUB, neutralLabel, editionLabel?, promo? }`
Ответ (серая зона): `{ status: "ambiguous", candidates: [...] }`
Ответ (не нашли): `{ status: "not_found", suggestion: "manual_request" }`
Ответ (Sony down): `{ status: "store_unavailable", managerLink }`
Ответ (rate limit): `{ error: "rate_limited", message, retry_after }`

### POST /api/price-request
Принимает заявку: `{ gameName, contact, contactType }`. Пишет в `price_lookup_requests`. Опционально шлёт уведомление админу (на старте можно без, потом TG-бот).

### GET /test/product/:region/:conceptId
Отладочный — парсит одну страницу товара. Использовать ТОЛЬКО для разработки парсера. В проде отключить или закрыть авторизацией.

### GET /health
Проверка живости. Возвращает `{ status: "ok", service: "price-lookup", version }`.

## Правила работы с кодом

### Стиль
- Современный JavaScript, ES Modules, async/await
- Без TypeScript (проект небольшой)
- Без лишних абстракций
- Комментарии на русском там где есть смысл объяснить «почему», а не «что»

### Парсинг — defensive
- HTML от Sony может быть каким угодно — всегда try/catch
- Селекторы могут не найти элемент — проверяй на null перед использованием
- Цена может быть «Not available», «Coming soon», «Free» — обрабатывай явно
- Парсеры цены (parseTRPrice/parseINPrice) возвращают `null` если не нашли — не падай
- Логируй проблемы парсинга в console (потом смотрим что Sony поменяла)

### Fuzzy matching
- Нормализация запроса перед поиском: lowercase, trim, убрать пунктуацию, римские → арабские
- Пороги схожести: ≥0.85 автомат, 0.5-0.85 серая зона, <0.5 не нашли
- Для кириллицы порог понижаем (PSN сам умеет в кросс-язычный поиск, доверяем больше ему)

### Защита от блокировок Sony (КРИТИЧНО)
- **User-Agent** — реалистичный Chrome on Windows (уже в коде)
- **Accept-Language** — под регион (en для en-tr, en для en-in)
- **Кэш 24 часа** в Supabase — критично, без него быстро забанят
- **Rate limiting** на эндпоинте — пользователь не должен дёрнуть Sony 100 раз/час через нас
- **Рандомная задержка** 200-800ms между запросами если их много

### Защита механики ценообразования (бизнес-критично)
- НЕ показывай клиенту регион (TR/IN) откуда цена
- НЕ показывай локальную цену (TRY/INR) — только в RUB
- НЕ объясняй как считается наценка
- Формулировка ВСЕГДА «ориентировочно от X ₽», не «точная цена», не «лучшая цена»

## Чего НЕ делать

- **Не дёргай Sony без кэширования** — забанят
- **Не указывай клиенту регион/локальную цену** — защита механики
- **Не используй Selenium/Puppeteer без крайней необходимости** — медленно
- **Не показывай DLC/аддоны в результатах** — фильтр по `productType: GAME`
- **Не показывай игры с маркером "Not available for purchase" / "Included" (PS Plus)** — это не покупка
- **Не давай скидки и не обещай ничего** — компетенция менеджера
- **Не храни сырой IP** — только sha256-hash
- **Не используй наценку отличную от магазина** — единая логика ценообразования через `markupTiers` из настроек

## Связь с другими сервисами проекта

- **Магазин (основной)** — фронт встраивает виджет «Не нашли игру?». Price-lookup читает курс и `markupTiers` из общей Supabase. Кнопка «Хочу купить» → добавление в корзину магазина
- **chatbot** — главный источник трафика. Когда бот рекомендует игру не из каталога, клиент попадает в price-lookup с UTM-меткой `?ref=chatbot&session=X&game=Y`. Это логируется в общую `event_tracking`
- **Магазин-описания** — не связаны напрямую

## Workflow

- На старте можно работать прямо в `main`
- Когда парсер начнёт работать и пойдёт усложнение — переход на feature-ветки
- Удалённый репозиторий — приватный (парсинг чужого сайта = серая зона, не светим публично)

## Перед изменениями

1. Прочитай `BRIEFING.md` если меняешь бизнес-логику или архитектуру
2. Если меняешь парсер — проверь на нескольких реальных играх из `TEST_QUERIES` в `server.js`
3. Если добавляешь новый запрос к Sony — убедись что есть кэш и rate limit
4. Если меняешь форматы цен — проверь на крайних случаях (бесплатные игры, скидки, нет в продаже)
5. Если работаешь с пользовательскими данными — IP только в hash

## Что точно НЕ делать без явного запроса Дениса

- Менять структуру таблиц Supabase
- Менять `MANAGER_PRIMARY_*` контакты (готовность к in-site chat — через конфиг, не код)
- Менять диапазоны `markupTiers` (это настройки магазина, общие)
- Деплоить на VPS
- Создавать платные аккаунты где-либо (psprices, deku, прокси-сервисы и т.д.)
- Переходить на Selenium/Puppeteer (если простой fetch+cheerio не справляется — сначала обсудить)

## Тестовые запросы

В `server.js` массив `TEST_QUERIES`:
- `Cyberpunk 2077` — точное совпадение
- `Forza Horizon 5` — свежий релиз (апрель 2025), проверяет новые игры
- `GTA V` — короткое название, много edition
- `FIFA 24` — ежегодный франчайз, легко промахнуться
- `Spider-Man` — частичное (Marvel's Spider-Man / Spider-Man 2 / Remastered)
- `Ведьмак 3` — кириллица
- `киберпанк` — кириллица, нижний регистр, без номера

После любых изменений парсера — прогнать все 7 через `/api/price-lookup` и убедиться что не сломалось.

---

## Идеи развития (аудит Fable)

Приоритет: влияние на конверсию/деньги.

### ✅ Уже реализовано
- **#1 Следить за ценой** — ✅ закрыт (сессии 19–21). Детали — в `AUDITS-ARCHIVE.md` (команда `/session-audits-archive`). ⚠️ Открытые баги фичи — PL-16/17/19/6 (см. аудит сессии 26 выше).

### 🔴 Высокий приоритет (деньги)
- **#2 Срок скидки** — PS Store отдаёт дату окончания акции. Показывать «−60% до 24 июня» в результате → триггер срочности. Данные уже есть в `__NEXT_DATA__`, нужно достать и пробросить в `_details` + UI.
- **#11 Поиск прямо в TG-боте** — chatbot уже есть, добавить команду: пользователь пишет название → бот дёргает `/api/price-lookup` → отвечает ценой + кнопкой. Технически просто — один `fetch` в `tg-bot.js`.
- **#13 Статус заявки** — после «Заявка принята» автоответ в TG: «Заявка №47 принята, ответим в течение 2 часов». Клиент не уходит за ожидание. `price_lookup_requests.id` уже есть.

### 🟡 Средний приоритет (UX/доверие)
- **#14 Дата обновления цены** — строчка «Цена обновлена сегодня в 14:30» из `fetched_at` кэша. Снимает недоверие к «ориентировочной» цене. Минимум кода.
- **#6 Рейтинг игры** — IGDB отдаёт оценку. «★ 4.5» рядом с ценой — снимает сомнение «а стоит ли». Уже дёргаем IGDB для обложки.
- **#12 Кнопка «Поделиться»** — `?game=X` уже автозапускает поиск. Добавить кнопку копирования ссылки в результат. Бесплатный вирусный трафик.
- **#3 Предзаказы** — невышедшая игра = `not_found` сейчас. IGDB отдаёт дату релиза. Показывать «Выходит 18 сентября — оставить предзаказ» вместо тупика.

### 💡 Идеи на будущее
- **#4 Апселл подписки** — если игра входит в PS Plus Extra/Premium, показывать рядом с ценой. Требует каталог PS Plus (ручной или парсинг).
- **#7 Demand → контент** — editorial-bot автоматически черновикует «Топ-5 искали на неделе + цены» из таблицы `not_found`. Соединить два существующих сервиса.
- **#8 «Что подешевело» рубрика** — чекер из watch-фичи знает все скидки по кэшу. Раз в неделю пост в TG/VK. Канал начинает продавать.
- **#9 Авто-пополнение каталога** — кнопка в админке «добавить в каталог» напротив топа `not_found` запросов.

---

## Лог сессий

<!-- Новые записи добавляются командой /session-end В НАЧАЛО этого раздела. -->

### 2026-07-23 (сессия 33 — price-lookup как бесшовный фолбэк в обычном поиске магазина)

**Контекст:** Денис спросил, можно ли объединить price-lookup с обычным поиском магазина: клиент вводит игру в единое окно поиска, при совпадении с каталогом — как сейчас, а если каталог по запросу пуст — предлагать найти через price-lookup (для клиента это один поиск, он не знает про отдельный сервис). Нижний standalone-блок «Не нашли игру?» пока оставить — дублируем, судьбу решим позже.

**Что сделано:**
- **Спроектирован контракт интеграции** и передан сессии магазина (реализация — в `js/client.js` репо poigraem; сам price-lookup не трогали — эндпоинты `/api/suggest` и `/api/price-lookup` уже есть).
- **Главное архитектурное решение — стоимость запроса.** Поиск магазина срабатывает на каждую букву; у price-lookup два разных по цене эндпоинта: `/api/suggest` (IGDB, дёшево, 120/час) и `/api/price-lookup` (живой скрейп Sony, дорого, 10/час, риск бана). Категорически нельзя привязывать дорогой запрос к вводу — за один заход клиент сжёг бы весь лимит и приблизил бан. Выбран путь: показывать **кнопку-CTA** при пустом каталоге, дорогой запрос уходит **строго по клику**.
- **Развилки закрыты выбором Дениса:** (1) триггер — «карточка-CTA по клику» (не автозапуск, не IGDB-подсказки на ввод); (2) показ результата — «переиспользовать модалку `plm*`», не рисовать третью копию UI (нас дважды жёг разъезд копий модалки, PL-24); (3) текст кнопки — после пяти отклонённых вариантов Денис задал свой: **«Игры нет в каталоге? Жми сюда»**.
- **Реализация (сессия магазина, `js/client.js`, коммит `173e9e9`):** `_schedulePlCta(q)` + `_plCtaOpen()` (дебаунс 350 мс, показ только при запросе ≥2 симв., на показе никаких запросов); контейнер `<div id="cli-pl-cta">` + вызов `_schedulePlCta(q)` в пустой ветке `renderClientList` (`!f.length`); по клику — `openPriceModal(clientQuery)` (функция уже была, сама подставляет запрос и запускает `plmSearch()`). Новая буква перезапускает таймер (кнопка не мерцает); появилось совпадение в каталоге → список перерисован → `if(!box) return` гасит отложенный таймер. `node --check` чист.
- **Залито и запушено** (коммиты `173e9e9` — фолбэк, `dec68ba` — session-end магазина + ротация лога, оба в репо poigraem). **Работает на живом сайте — подтвердил Денис.**

**Что решили:**
- Триггер = CTA по клику; дорогой запрос к Sony — только по явному клику, никогда по ходу набора (защита лимита и от бана).
- Дебаунс 350 мс оставлен как есть (Денис уточнял — не многовато ли; разъяснено, что это 0.35 с «клиент перестал печатать», не задержка ожидания; вариант 200 мс отклонён).
- Модалку не переписываем — только вход с готовым запросом через существующий `openPriceModal` (паритет двух копий модалки, урок PL-24).
- Нижний блок «Не нашли игру?» пока дублируется осознанно — что с ним делать, Денис решит позже.

**Новые баги:** нет (интеграция, price-lookup не менялся).

**Открытые вопросы:**
- Вопросы по **дизайну** кнопки/пустой выдачи — Денис решает отдельно через claude design.
- Судьба нижнего standalone-блока «Не нашли игру?» (убрать/оставить) — отложено.

**Что осталось:**
1. 🎨 Дизайн CTA/пустой выдачи (claude design, на стороне Дениса)
2. 🚚 PL-24 — визуальная приёмка на iPhone (код подтверждён живым)
3. 👁 Клик-тесты PL-16/PL-17, вид баннера `priceUncertain`
4. 🔴 PL-25 (одна строка), PL-26 (дедуп алерта), PL-4 (таймаут fetch)
5. 🟡 Остаток мелочей PL-9/10+Rs./12/18/20/21/22/27…30
6. ⚠️ `price_lookup_log` в Supabase (тянется с сессии 24)
7. 👁 Наблюдения: доля «ушедших в Индию» (PL-33), пороги `TRUST_CONF`/`TRUST_GREY` (PL-31); прежние хвосты (детектор поломки, #2/#11, идеи развития)

### 2026-07-20 (сессия 32 — приёмка всех функций, IGDB-диагноз для магазина, PL-34 (словарь Hogwarts) + PL-35 (бандл-как-издание), путь Б по продукту)

**Контекст:** Денис попросил прогнать проверку всех функций price-lookup (планирует выводить свои инструменты на продажу для микробизнеса) и подумать, как масштабировать price-lookup как продукт — реально ли продавать его как «парсер цен конкурентов».

**Что сделано:**
- **Функциональная приёмка (локальные тесты + живые запросы к VPS):** юнит-тесты 42/42 (секции recon/ — только на VPS, ожидаемо); живым HTTP подтверждены `/health`, CORS-preflight 204 (PL-15), «киберпанк» → 2610 ₽ с изданиями/скидкой, Wolverine → 6350 ₽ (маркер PL-32/33), «резидент ивел 4» → честный not_found (PL-31), пустой gameName → 400 (PL-2), валидация `priceRub` → 400 (PL-19), админ без токена → 401. Вердикт: всё работает как задумано. `/api/price-request` живьём не дёргался (создал бы реальную заявку).
- **Найдена и диагностирована деградация IGDB (сторона магазина):** `igdb/suggest` отдавал `[]` на любой запрос при живом `igdb/cover` — по `category: null` у Elden Ring поставлен диагноз: IGDB завершила миграцию `category` → `game_type`, старое поле принимается, но пусто → `where category = (0,8,9)` не матчит ничего, ошибок в логах нет (пустой catch). Побочно у нас были мертвы: автоподсказки виджета и IGDB-перевод кириллицы (`server.js:377` — запросы вне словаря падали в хрупкую trustTop-ветку), плюс деградировало ранжирование обложек в `/igdb/cover` (без category нет бонуса main game/штрафа DLC). Составлено детальное сообщение сессии магазина (3 точки правки в их `backend/server.js`) — **починено их коммитом `c1fdd3d`**, VPS-агент предварительно подтвердил гипотезу по сырому телу ответа IGDB в pm2-логах. С нашей стороны подтверждено: suggest вернул Elden Ring, «ведьмак 3» → «The Witcher 3: Wild Hunt». Их бэклог: логировать не-массивные ответы IGDB (наша рекомендация, не сделано).
- **PL-34 — словарь Hogwarts (`server.js`, коммит `54e4735`, задеплоен+подтверждён):** «хогвартс легаси» давал not_found даже после починки IGDB — у Hogwarts Legacy нет русского альт-имени в IGDB (проверено: и «хогвартс легаси», и транслит «rezident…»-класса запросы IGDB не матчит). Единственный надёжный путь — словарь: 4 строки в `CYRILLIC_TRANSLATE` («хогвартс легаси», «хогвартс наследие», «хогвардс», «хогвартс» → `hogwarts legacy`). VPS-лог подтвердил перевод.
- **PL-35 — бандл-как-издание (`parsers.js` + `test-parsers.js`, коммит `df8919d`, задеплоен+подтверждён):** после словаря запрос давал `ambiguous` вместо `found` — в PS Store нет SKU «Hogwarts Legacy», база разбита на «PS4 Version»/«PS5 Version», и бандл «Hogwarts Legacy + Harry Potter: Quidditch Champions…» один ронял `allSameGame` (слова «harry»/«quidditch» — не edition-слова). Воспроизведено локально на синтетических кандидатах. По решению Дениса (отклонён вариант «отсекать бандлы по +») бандл — законное издание игры: новый `isBundleWithGame(queryNorm, rawName)` — сегмент сырого имени до «+» должен сам быть изданием запрошенной игры (перевёрнутый «Y + X» не считается); подключён в `allSameGame` через `||`. Бандл остаётся в списке изданий и ценовом пуле. Юнит-тест 2e (5/5): found, best = дешевейшая база, бандл в alternatives, контроль «far cry» (сиквелы без «+») остался ambiguous. Живой прогон после деплоя: «хогвартс легаси» → found, база PS4 1040 ₽, в изданиях PS5-версия и бандл.
- **Продуктовый разбор (по запросу Дениса):** price-lookup как универсальный «парсер цен конкурентов» продавать нереально (каждый сайт = свой парсер + антибот + вечная поддержка; рынок занят Priceva/uXprice/Competera; юр. риски при продаже скрейпинга). Реальный путь — переупаковка в «модуль спроса и цен» (подробно — п. 9 «Ближайших задач»).

**Что решили:**
- **Путь Б по продукту** (выбор Дениса из трёх вариантов): модуль спроса и цен с плагинами источников цены, не универсальный парсер и не публичная продажа PS-парсера. Порядок вывода: чат-бот → avito-helper/content-creator → модуль. Зафиксировано и в памяти Claude (`product-path-b.md`).
- **Бандл «игра + другой товар» = издание игры** (семантика Дениса, отклонён мой вариант фильтрации по «+»): содержит саму игру в отличие от апгрейда PL-32, участвует в изданиях и цене. Если подешевевший бандл когда-то выиграет минимум цены — это осознанно корректное поведение.
- Фонетическая кириллица вне словаря и вне IGDB-альт-имён («резидент ивел 4») — потолок данных: остаётся честный not_found, лечится пополнением словаря по мере жалоб. Это лучше, чем угадывать.
- trustTop-ветку сознательно не трогали (бандл в её alternatives не попадёт) — без живого кейса не расширяем.

**Новые баги:** PL-34, PL-35 — найдены и закрыты в этой же сессии (детали в `AUDITS-ARCHIVE.md`). Плюс чужой (магазина): IGDB `category` → `game_type`, закрыт их коммитом `c1fdd3d`.

**Открытые вопросы:**
- Логирование не-массивных ответов IGDB в suggest магазина — в бэклоге сессии poigraem, напомнить при случае.
- Инцидент харнесса: инструмент Grep в середине сессии сломался («Executable not found»), обходился PowerShell Select-String — если повторится, это не проблема репо.

**Что осталось:**
1. 🚚 PL-24 — визуальная приёмка на iPhone (код подтверждён живым)
2. 👁 Клик-тесты PL-16/PL-17, вид баннера `priceUncertain`
3. 🔴 PL-25 (одна строка), PL-26 (дедуп алерта), PL-4 (таймаут fetch)
4. 🟡 Остаток мелочей PL-9/10+Rs./12/18/20/21/22/27…30
5. ⚠️ `price_lookup_log` в Supabase (тянется с сессии 24)
6. 👁 Наблюдения: доля «ушедших в Индию» (PL-33), пороги `TRUST_CONF`/`TRUST_GREY` (PL-31); словарь пополнять по жалобам; прежние хвосты (детектор поломки, #2/#11, идеи развития)

### 2026-07-17 (сессия 31 — фиксы PL-32 (апгрейд-DLC в цене) + PL-33 (порог региона 20%→12%), оба задеплоены и закрыты)

**Контекст:** Денис сообщил, что новая функция магазина «дозаполнить цены» (прайсит закинутые без цены игры) выдала для Marvel's Wolverine 1010 ₽ — подставилась цена не той игры. Claude воспроизвёл на живых страницах PS Store, что виноват парсер price-lookup, а не магазин, и по ходу разбора нашёл второй, отдельный баг ценообразования.

**Что сделано:**
- **PL-32 — апгрейд-DLC выигрывал выбор минимальной цены (`parsers.js`, коммит `1b06454`, задеплоен+подтверждён).** Живой прогон TR+IN: по запросу «Marvel's Wolverine» Sony отдаёт саму игру (FULL_GAME, 3449 TL), Deluxe (PREMIUM_EDITION, 3849 TL) и **«…Digital Deluxe Edition Upgrade» (GAME_BUNDLE, 400 TL)**. Апгрейд по classification неотличим от Ultimate-издания → проходил фильтр «только игры», а `isEditionOfSameGame` роднил его с игрой → все три попадали в один ценовой пул → выбор минимума забирал апгрейд за 400 TL (≈1010 ₽). Две причины, обе нужны: (1) `GAME_BUNDLE` в белом списке; (2) `normalizeQuery("Marvel's Wolverine")` из-за апострофа давал «marvel s wolverine» — 3 слова, из-за чего срабатывало правило «≥3 слов → остаток подзаголовок». Фикс двумя заходами: **А** — `UPGRADE_RE = /\bupgrade\b/` отсекает кандидатов со словом upgrade в имени (если сам клиент апгрейд не искал), стоит в первичном фильтре до развилки trustTop; **Б** — апострофы (`'’‘\`´`) удаляются в `normalizeQuery` без пробела («marvels wolverine», 2 слова), заодно чинит биграммное сходство тайтлам Assassin's/Tom Clancy's. Новый юнит-тест 2d (6/6), обновлён ассерт normalizeQuery (`assassins creed`). Регрессия на живых страницах Wolverine/Spider-Man 2/AC Mirage/Hogwarts прогнана через версию из HEAD и исправленную — изменился ровно Wolverine.
- **PL-33 — порог TR-приоритета 20% → 12% (`server.js`, коммит `394bd52`, задеплоен+подтверждён).** Обе цены (TR/IN) конвертируются в рубли, дальше берётся не минимум: Турция приоритетна, Индия побеждает только если дешевле на ≥X% (`inRub <= trRub * factor`). Класс новых игр вида 3449 TL / 4999 ₹ имеет разрыв ~13.7% — при пороге 20% Индия проигрывала, клиент видел завышенную турецкую цену. Порог опущен до 12% (Wolverine: TR 7360 → IN 6350, запас ~127 ₽ над классом). Порог вынесен в единую константу `TR_PRIORITY_FACTOR = 1 - 0.12`, применяется и в выборе winner, и в дедупликации изданий (раньше два литерала `0.8`). Правило задокументировано в CLAUDE.md (раздел «Выбор региона» в «Архитектурных решениях») — раньше его не было нигде.
- **Незакоммиченный `/session-end` сессии 30 закоммичен** (коммит `898e45f`): VPS-агент при деплое PL-32 читал CLAUDE.md с GitHub, где доки отставали на 13 июля, и принял деплой PL-31 за недокументированный — оказалось, session-end сессии 30 просто висел незакоммиченным. Заодно в шаблон деплоя внесена поправка: `node --check файл.new` НЕ работает при `"type": "module"` (Node не выводит модульность для расширения `.new`), проверять синтаксис на копии `.mjs` (нашёл VPS-агент).
- **Деплой обоих фиксов (VPS-агент, доказательные отчёты):** PL-32 — 1010 ₽ (апгрейд) → 7360 ₽ (сама игра), тесты 6/6, регрессия чистая. PL-33 — 7360 ₽ (TR) → 6350 ₽ (IN), `[getSettings] rate=1.76 inrGift=1050` из Supabase совпал с расчётным, три контрольные игры остались в Турции (там дешевле). Дрейфа VPS↔репо нет — деплой был обновлением из origin/main.

**Что решили:**
- Оба фикса (А+Б для PL-32) реализованы одним заходом по рекомендации Claude и выбору Дениса; вариант «ужесточить правило ≥3 слов» отклонён как рискованный (задевает легитимные «The Witcher 3 Wild Hunt»).
- Порог PL-33 = 12% в **текущей** формуле (процент от турецкой, большей цены — как все проценты в проекте). Разобрано, что формулы «Индия дешевле TR на X%» и «TR дороже IN на X%» эквивалентны, но дают разные числа на одном разрыве (13.72% vs 15.91%); чтобы правило не поехало молча, знаменатель зафиксирован в коде и доке. 12% выбран из-за запаса ~127 ₽ над маркерным классом (13% давал всего 53 ₽ — при движении курса игры прыгали бы между регионами).
- PL-32 и PL-33 закрыты полностью (задеплоены + живой прогон) → перенесены в `AUDITS-ARCHIVE.md`, в отличие от прошлых «код готов, но не проверено».
- Калибровка порога сделана на подтверждённых числах магазина (`rate=1.76`, `inrGift=1050`), не на прикидке — VPS-агент прислал их из живого `getSettings`.

**Новые баги:** PL-32, PL-33 — найдены и закрыты в этой же сессии (см. `AUDITS-ARCHIVE.md`).

**Открытые вопросы:**
- Порог `TR_PRIORITY_FACTOR = 0.88` откалиброван под один класс игр — стоит последить по логам `[winner]`, какая доля каталога уехала в Индию в зоне 12–20% и не задело ли лишнего.
- `price_lookup_log` в Supabase по-прежнему не подтверждена (тянется с сессии 24).

**Что осталось:**
1. 🚚 PL-24 — визуальная приёмка на iPhone (код подтверждён живым)
2. 👁 Клик-тесты PL-16/PL-17, вид баннера `priceUncertain`
3. 🔴 PL-25 (одна строка), PL-26 (дедуп алерта), PL-4 (таймаут fetch)
4. 🟡 Остаток мелочей PL-9/10+Rs./12/18/20/21/22/27…30
5. ⚠️ `price_lookup_log` в Supabase
6. 👁 Наблюдение за долей «ушедших в Индию» после PL-33; прежние хвосты (детектор поломки, #2/#11, идеи развития)

### 2026-07-14 (сессия 30 — приёмка пакетов на живом сайте, находка+фикс PL-31, закрытие PL-23)

**Контекст:** Денис попросил провести приёмку пакетов сессии 28/29 на живом сайте, так как у него нет iPhone для клик-теста «Следить за скидкой». Просил Claude выполнить приёмку самостоятельно — «на живом сайте или через локальный сервер».

**Что сделано:**
- **Честно оценены реальные возможности приёмки:** у Claude нет браузерного/click-инструмента (Playwright и т.п. не подключены) и нет локального `.env` (секреты Supabase не в репозитории, локальный сервер поднять было не из чего) — значит ни реальный клик-тест, ни попап-тест на iOS Safari не воспроизвести. Вместо этого сделана честная API-уровня приёмка живого сайта через curl/PowerShell и WebFetch, с явным проговариванием, что именно проверено и что нет.
- **Подтверждено живым HTTP-запросом на `api.poigraem.shop/price-lookup/`:**
  - PL-15 (CORS) — `OPTIONS /api/price-lookup` → `204` с корректным `Access-Control-Allow-Origin`, до лимитера.
  - PL-19 (валидация watch) — `priceRub: 999999999` → `400`, запись в БД не создаётся.
  - PL-2 (форма ошибки) — некорректный запрос → `400 {"error":...}` без `status`, что и ожидает исправленный `render()`.
- **Подтверждено живым чтением `poigraem.shop/js/client.js` (WebFetch + прямой curl сырого файла):** все три части PL-24 задеплоены и корректны — `plmWatch` открывает `window.open('','_blank')` синхронно до `await`, `plmRender` показывает честную ошибку вместо ложного «не нашли», баннер `priceUncertain`/`_plmWarnHtml()` на месте. Визуальной/боевой проверки на реальном iPhone не было — PL-24 остаётся формально открытым.
- **PL-17 подтверждён статическим чтением кода** (`_watchedNames` очищается только в `render()`, не в `selectEdition()`) — логика верна, но не кликнута живьём.
- **Найден и тут же исправлен новый критичный баг — PL-31.** В процессе живого прогона нескольких кириллических запросов (пробовал закрыть PL-23 живым trustTop-прогоном) запрос «резидент ивел 4» дважды подряд вернул `found` с `neutralLabel: "4PGP"` — посторонний товар вместо Resident Evil 4, с реальной ценой 1990₽. Причина найдена в `parsers.js:341-343`: в режиме `trustTop` `best = eligible[0]` брался безусловно, без проверки релевантности запросу (там сознательно пропускается биграммное `similarity()`, т.к. кириллица vs латиница никогда не пересекается по биграммам) — фикс PL-23 защищал только `alternatives` относительно `best`, сам `best` не проверялся никогда.
- **Обсуждение решения:** предложено 3 варианта (транслитерация+rerank / расширение того же на топ-N / кросс-проверка TR vs IN), дал рекомендацию с обоснованием. Денис выбрал вариант «переранжировать по транслит-сходству, не брать eligible[0] слепо».
- **Реализован фикс PL-31** (`parsers.js`): новая `translitCyrillic()` (фонетическая карта кириллица→латиница), в trustTop-ветке кандидаты переранжируются по `similarity(translit(query), name)` с порогами `TRUST_CONF=0.45` (found) / `TRUST_GREY=0.28` (ambiguous) / ниже → `not_found`. Фильтр изданий PL-23 вынесен в переиспользуемый хелпер `trustTopEditions()`.
- **Тесты:** новый юнит-тест 2c (PL-31, синтетические кандидаты, не требует `recon/`) — 5/5; тест 2b (PL-23) — по-прежнему 4/4, регрессии нет.
- **Коммит и деплой:** закоммичены только `parsers.js` + `test-parsers.js` (коммит `74854ae`), запушено на `origin/main` (fast-forward, без конфликтов). Написана и передана VPS-агенту пошаговая инструкция деплоя (по шаблону сессии 29: `cd ~/price-lookup` обязателен, `mv` отдельными командами, diff-предохранитель, живой прогон после рестарта).
- **VPS-агент выполнил деплой и прислал доказательный отчёт:** маркеры/синтаксис/diff чисты, юнит-тесты 4/4+5/5 на живом файле, `pm2 restart` без ошибок. Живой прогон «резидент ивел 4» ДО рестарта (старый код) — дважды подряд `4PGP` с ценой (баг воспроизведён на проде); ПОСЛЕ рестарта — тот же сырой кандидат `4PGP` от Sony по-прежнему приходит, но rerank его отклоняет → честный `not_found`. Этот прогон одновременно закрыл PL-31 и снял единственное препятствие для закрытия PL-23 («живой trustTop-прогон не проводился»).
- **PL-23 и PL-31 перенесены в `AUDITS-ARCHIVE.md`** с подробностями (было/фикс/подтверждение), в CLAUDE.md — строки-указатели.
- **Архивация:** сессия 24 (самая старая из 6 записей лога) перенесена в начало `SESSIONS-ARCHIVE.md`, маркер обновлён на «Сессии 1–24».

**Что решили:**
- Приёмка без браузера/устройства — честно разграничивать «подтверждено кодом+HTTP» и «нужен реальный клик/экран», не выдавать одно за другое.
- Вариант «транслитерация + переранжирование по сходству» — сильнее закрывает корень PL-31, чем просто проверка `best` без ранжирования (вариант 1) или кросс-проверка TR/IN (вариант 3, ненадёжна из-за легитимных региональных расхождений каталога).
- Живой прогон trustTop-ветки для PL-31 засчитан и как формальное закрытие PL-23 — единственным препятствием было именно отсутствие такого прогона.
- Пороги `TRUST_CONF`/`TRUST_GREY` — осознанная эвристика на здравом смысле и синтетических тестах, не калибровка на реальном трафике; помечено как открытый хвост для наблюдения.
- Незакоммиченный `/session-end` сессии 29 (правки CLAUDE.md/SESSIONS-ARCHIVE.md) не трогать отдельным коммитом — уедет вместе с этой записью.

**Новые баги:** PL-31 — найден и закрыт в этой же сессии (см. выше и `AUDITS-ARCHIVE.md`).

**Открытые вопросы:**
- Реальный клик-тест PL-16 (попап на iOS Safari) и PL-17 (переключение изданий) — нужен физический iPhone или браузерный/click-инструмент, которого пока нет ни у Дениса под рукой, ни у Claude.
- Пороги `TRUST_CONF=0.45`/`TRUST_GREY=0.28` не откалиброваны на реальном трафике — стоит последить за логами `sendTgAlert`/долей `ambiguous`-ответов первое время после накопления трафика.

**Что осталось:**
1. 🚚 PL-24 — визуальная приёмка на iPhone (код подтверждён живым)
2. 👁 Визуальная приёмка кликами того, что Claude не мог проверить без браузера: PL-16/PL-17 живьём, вид баннера `priceUncertain`
3. 🔴 PL-25 (одна строка), PL-26 (дедуп алерта), PL-4 (таймаут fetch)
4. 🟡 Остаток мелочей PL-9/10+Rs./12/18/20/21/22/27…30
5. ⚠️ `price_lookup_log` в Supabase — по-прежнему не подтверждена
6. Прежние хвосты (детектор поломки, #2/#11, идеи развития)

### 2026-07-13 (сессия 29 — расширенный аудит Fable №4 (PL-23…30), skill аудита, фиксы PL-24 + PL-23, деплой PL-23)

**Контекст:** Денис попросил провести новый расширенный аудит по образцу UX-аудита магазина (приоритеты: точный парсинг, просчёт, красиво/удобно клиенту, стройная архитектура), оформить методику в skill, зафиксировать находки и починить приоритетное.

**Что сделано:**
- **Аудит №4 (PL-23…PL-30):** сплошное чтение всех 5 файлов сервиса + сверка с `js/client.js` магазина. Фиксы сессии 28 подтверждены стоящими в коде; все старые открытые находки перепроверены и подтверждены. 8 новых находок — полный текст в разделе «Аудит Fable (2026-07-13)». Главные: PL-23 (trustTop подмешивал чужие игры в ценовой пул), PL-24 (виджет магазина не получил фиксы PL-16/PL-2/priceUncertain — расхождение двух копий модалки).
- **Skill `/price-lookup-audit`** — методика аудита (персоны, 10 сквозных критериев, формат находок) в `.claude/skills/price-lookup-audit/SKILL.md`. Локальный, в git сознательно НЕ коммитится. В CLAUDE.md добавлен раздел «Как проводить аудит».
- **PL-24 реализован** (репо poigraem, НЕ закоммичен — коммитит сессия магазина): `plmWatch` — `window.open('','_blank')` синхронно + `win.location` после ответа (iOS); `plmRender` — форма «не нашли» только при `status==='not_found'`, иначе честная ошибка; баннер `priceUncertain` (`_plmPriceUncertain`/`_plmWarnHtml()` + `.plm-warn` в `styles.css`). `node --check` чисто. ⚠️ В poigraem параллельная сессия (тег `?v=20260713ux2`, комментарии UX2-18) — cache-bust не трогал.
- **PL-23 реализован и задеплоен** (коммит `0b2ef15`): `isEditionOfSameGame` получил параметр `strict` (отключает правило «≥3 слов → подзаголовок» — при сравнении двух полных имён товаров оно ложно роднит сиквелы); в trustTop-ветке `matchGame` alternatives фильтруются двусторонней strict-проверкой. Юнит-тест 2b в `test-parsers.js` (синтетические кандидаты, работает без `recon/`): 4/4 локально и на живом файле VPS. Деплой прошёл со второй попытки — первая ушла в `~` вместо `~/price-lookup` (сигнал: огромный diff), домашние файлы почищены.
- **Тест GTA V починен** (коммит `eec6cc4`): ассерт `'gta 5'` устарел с появлением ABBR-словаря (`gta → grand theft auto`, намеренное симметричное поведение) → `'grand theft auto 5'`. Секция 2 теперь 7/7.
- **`recon/` восстановлен на VPS** свежими страницами PS Store (папка в `.gitignore`, потерялась при выносе price-lookup в отдельный репо) — секции 3–8 тестов ожили, матчинг Ultimate/PS4/PS5 работает.
- **Инфра:** git на этой машине — только встроенный в GitHub Desktop (`C:\Users\enter\AppData\Local\GitHubDesktop\app-3.6.1\resources\app\git\cmd\git.exe`); identity задана локально в репо (`Denis <spb.taximaxim@gmail.com>`, глобального .gitconfig нет). На origin обнаружился чужой коммит `77f11d8` («bind 127.0.0.1») — rebase чистый.

**Что решили:**
- Аудиты — только локально, VPS — только деплой; `.claude/skills/` в git не коммитим.
- PL-24 коммитит и деплоит сессия магазина (правки лежат в её рабочей копии).
- PL-23 формально НЕ закрыт до живого прогона trustTop-ветки (юнит-тест на задеплоенном файле есть, боевого кириллического запроса вне словаря не было).
- ⚠️ Поправка к логу сессии 28: «убран мёртвый tg://resolve на мобильных» относилось только к `watchGame`; в `buyButtons` (`index.html:464-467`) `tg://` остался — это PL-25.

**Новые баги:** PL-23…PL-30 — см. раздел аудита. Процессное: деплой без `cd ~/price-lookup` уходит в `~` (шаблон деплоя в «Фактах инфры» дополнен).

**Открытые вопросы:**
- Коммит+деплой+приёмка PL-24 на стороне сессии магазина (iPhone-подписка, ошибки, баннер); кто двигает cache-bust `?v=`.
- Живой trustTop-прогон для закрытия PL-23.
- `price_lookup_log` в Supabase — всё ещё не подтверждена (с сессии 24).

**Что осталось:** PL-25 (одна строка) → PL-26 → PL-4; мелочи PL-9/10+Rs./12/18/20/21/22/27…30; визуальная приёмка пакетов сессии 28; прежние хвосты (детектор поломки, #2, #11, идеи развития).

<!-- Сессии 1–24, 26, 27, 28 (2026-05-27–2026-07-12) перенесены в SESSIONS-ARCHIVE.md -->
