# Sessions Archive — price-lookup

Старые записи лога, перенесённые из CLAUDE.md.

---

### 2026-07-07 (сессия 26 — третий аудит Fable, PL-15…PL-22 + брейншторм развития)

**Контекст:** Денис явно попросил провести аудит самой мощной моделью (Fable 5) — пройтись по коду price-lookup, проверить логику/неработающие функции, и отдельно подумать, как вывести инструмент на новый уровень (что есть у больших конкурентов вроде PSprices/GG.deals/DekuDeals). Правок кода в этой сессии НЕ было — только аудит и фиксация находок.

**Что сделано:**
- Сплошное чтение всех 4 файлов сервиса (`server.js`, `parsers.js`, `public/index.html`, `public/admin.html`) + сверка с точками вызова из основного репо магазина (`js/client.js:1703-1706`, `js/admin-images.js:31-32`, `js/admin-core.js:984`).
- Перепроверены по актуальному коду все 14 находок сессии 25 (PL-1…PL-14) — **подтверждены, ничего не исправлено** с прошлого аудита.
- Найдено 8 новых проблем, нумерация PL-15…PL-22 (полный текст — в разделе «🔍 Аудит Fable (2026-07-07, сессия 26)» в CLAUDE.md):
  - **PL-15 (критично)** — CORS-preflight OPTIONS считается лимитером наравне с реальным запросом (лимитеры смонтированы раньше CORS-middleware в `server.js`), а при исчерпании лимита сам OPTIONS падает без CORS-заголовков → браузер магазина видит не 429, а сетевую ошибку. Реальный бюджет клиента из магазина — 5 поисков/час вместо заявленных 10, и лимит достигается незаметно с ложной причиной на экране.
  - **PL-16, PL-17, PL-19** (watch-фича) — `window.open` после `await` ломает подписку на iOS Safari; переключение изданий в модалке сбрасывает состояние кнопки слежки; `/api/price-watch/init` не валидирует `priceRub` от клиента и не чистит брошенные pending-подписки.
  - **PL-18, PL-20, PL-21, PL-22** (мелочи) — demand-запрос в админке без сортировки станет случайной выборкой после 2000 строк; `'null'` в CORS-whitelist; неполное HTML/JS-экранирование в `h()`/`a()`; `refine()` без guard на повторный клик (гонка запросов).
- Составлен и передан Денису список идей развития — что есть у крупных агрегаторов и как это можно добавить дёшево, используя уже существующую инфраструктуру: история цен (таблица `price_history` параллельно с кэшем), срок скидки (уже в бэклоге #2), целевая цена в слежке (вместо зашитых −15%), SEO-страницы `/game/slug` из уже накопленных логов (сервис сейчас не виден поисковикам), «популярно сейчас» из логов поиска, поиск в TG-боте (#11).

**Что решили:**
- Приоритет исправлений — двумя пакетами: «воронка» (PL-15+PL-1+PL-2+PL-3+PL-5 — всё, что прямо сейчас теряет живых клиентов) первым, затем «watch-фича» (PL-16+PL-17+PL-19+PL-6 — чтобы подписки реально доживали до уведомления).
- Из идей развития выделен один рычаг с наибольшим потенциалом при небольших трудозатратах — SEO-страницы игр из уже накопленных данных: сервис сейчас существует только как виджет внутри магазина и невидим поисковикам, хотя вся база (`price_lookup_log`) для рендера таких страниц уже собирается сама.
- Денис сменил модель на Sonnet 5 сразу после получения отчёта — решение с чего начинать в этой же сессии не принято, зафиксировано как открытый вопрос.

**Новые баги:** PL-15…PL-22, см. раздел аудита в CLAUDE.md — все НЕ исправлены на момент сессии 26 (позже: PL-15/PL-2/PL-3/PL-5 — сессии 27–28, PL-19 — сессия 28).

**Открытые вопросы:**
- Денис не выбрал, с какого пакета начинать следующую сессию (воронка / watch-фича / что-то из идей развития).
- Существование таблицы `price_lookup_log` в Supabase по-прежнему не подтверждено (тянется с сессии 24) — без неё admin.html пуст независимо от фикса PL-1.

**Что осталось:**
1. Решить с Денисом стартовую точку: пакет «воронка» (PL-15/1/2/3/5, рекомендуемый приоритет — реальные потери конверсии прямо сейчас) / пакет «watch-фича» / идея развития (SEO-страницы или история цен)
2. Проверить таблицу `price_lookup_log` в Supabase
3. Прежние хвосты сессии 25 не сдвинулись (#2 срок скидки, #11 TG-поиск, детектор поломки парсера, негативный кэш)

---

### 2026-06-26 (сессия 24)

**Что сделано:**
- **Аудит кода: поиск скрытых багов и несоответствий** — полный проход по `server.js`, `parsers.js`, `public/index.html`, `public/admin.html`. Найдено 12 проблем.
- **Исправлен `aliasLatinQuery` (`server.js`)** — двойное расширение аббревиатур: "EA FC 26" → "ea ea sports fc 26", "EA Sports FC 26" → "ea sports ea sports fc 26". Причина: `endsWith(' ' + abbr)` срабатывал на запросы, где abbr стоит в конце более длинной строки. Фикс: убраны `endsWith` и `includes` из условий, добавлен guard `if (lower.includes(full)) continue`, добавлены явные записи `['ea fc 26', 'ea sports fc 26']` и `['ea fc 25', ...]`.
- **Исправлен `getSettings` catch (`server.js`)** — после первого падения `_settingsCachedAt` оставался `0`, TTL-проверка всегда давала false → каждый `convertToRub` лез в Supabase. Добавлен `_settingsCachedAt = Date.now()` в catch-блок.
- **Исправлен `checkWatchlist` startup (`server.js`)** — функция вызывалась только через `setInterval` (первый раз через 6ч), при частых `pm2 restart` фактически не работала. Добавлен немедленный вызов `checkWatchlist()` после `app.listen`.
- Коммит `a46faba`, задеплоено на VPS.

**Что решили:**
- Оставшиеся 5 мелких багов зафиксированы в «Текущем фокусе» как известные, не блокируют запуск.
- Проверка таблицы `price_lookup_log` в Supabase — на Денисе (если нет — создать).

**Новые баги (зафиксированы, не исправлены):**
- `sendRequest()` (`public/index.html`) — не передаёт `source`, все заявки 'direct'
- `checkWatchlist` — race condition двойного уведомления (notify ok, update fail)
- `NON_GAME_CLASSIFICATIONS` (`parsers.js`) — мёртвый код
- `watchGame()` (`public/index.html`) — нет проверки `r.ok`, ошибки 429/500 не показываются
- CSS `.m-ed-btn` (`public/index.html`) — мёртвый стиль после сессии 23

**Открытые вопросы:**
- Существует ли таблица `price_lookup_log` в Supabase? Без неё admin.html всегда пустой.

**Что осталось:**
- ⚠️ Проверить `price_lookup_log` в Supabase
- 💡 Детектор поломки парсера (TG-алерт)
- 💡 Негативный кэш `not_found`
- 🔴 #2 Срок скидки, #11 TG-бот поиск

### 2026-06-18 (сессия 23)

**Что сделано:**
- **Переключение изданий в standalone-модалке** (`public/index.html`). В модалке на `api.poigraem.shop/price-lookup/` строки изданий были статичными с отдельными кнопками «В корзину» на каждой. Переделано по аналогии с shop-функцией `_plmSelectEdition` (`js/client.js`): строки стали кликабельными переключателями (`selectEdition(i)`), при клике меняется верхняя главная обложка, название, цена и маркер `●`/`○`. Единые кнопки покупки/менеджера/слежки внизу работают по выбранному изданию. CSS: `.m-ed-row` получил `cursor:pointer`, `hover`, класс `.sel` (вместо старого `.m-ed-best`), добавлен `.m-ed-check`. JS: глобалы `_eds`/`_selIdx`, функция `selectEdition()`, `render()` вызывает `openModal()` + `selectEdition(0)` при `eds`. Убрана `m-ed-btn` (отдельная ссылка «В корзину» на каждой строке). Коммит `8aceeaa`, задеплоено на VPS.

**Что решили:**
- Standalone-модалка теперь зеркалит UX магазинной модалки: обе позволяют переключать издания с обновлением обложки. Различие: standalone использует редирект `poigraem.shop/?order=`, а магазин — реальную корзину.
- Обложки изданий PS Store (сессия 22) визуально подтверждены Денисом — реально различаются по изданиям. Задача обложек полностью закрыта.

**Новые баги:** не обнаружено.

**Открытые вопросы:** нет.

**Что осталось:**
- 💡 Детектор поломки парсера (TG-алерт при серии `not_found`)
- 💡 Негативный кэш `not_found`
- 🔴 #2 Срок скидки — «−60% до 24 июня» из `__NEXT_DATA__`
- 🔴 #11 Поиск прямо в TG-боте
- 🔴 #13 Статус заявки (автоответ в TG)

### 2026-06-18 (сессия 22)

**Что сделано (обложки PS Store по изданиям + 2 фикса):**
- **🔴 Обложки изданий из PS Store** — закрыт открытый вопрос сессии 21. Решено в пользу PS Store без прокси (точные по изданиям; URL `image.api.playstation.com` виден в DevTools — осознанный выбор Дениса). Реализация:
  - `parsers.js` — хелпер `pickCover(media, apollo)` достаёт box-art из `item.media` (приоритет ролей `PORTRAIT_BANNER → MASTER → GAMEHUB_COVER_ART → первая IMAGE`, видео-`PREVIEW` отсекается по `type`); поле `coverUrl` пишется в каждый кандидат. **0 лишних запросов** — данные уже в распарсенной странице, кэшируются в составе `result`.
  - `server.js` — у каждого издания своя `coverUrl`; верхняя обложка `found` и `ambiguous` берут PS-обложку, IGDB остаётся **фоллбэком** при пустом значении.
  - `public/index.html` — миниатюра 44px (`m-ed-cover`) в каждой строке издания + 🎮-плейсхолдер `onerror`.
  - Коммит `a03af65`. Проверено по API: Lies of P (`found` + издания с разными обложками), Astro Bot (`ambiguous`), Cyberpunk (старый кэш → IGDB-фоллбэк жив).
- **🐛 Баг лимитера автоподсказок.** `/api/suggest` сидел на строгом лимите `/api/price-lookup` (10/час). Подсказки бьют в IGDB (не Sony) и дёргаются на каждую букву → 2-3 поиска выбивали бюджет, клиент видел «слишком много запросов». Фикс: отдельный `suggestLimiter` (120/час, `SUGGEST_LIMIT_PER_HOUR`). Коммит `c641096`.
- **🐛 FC 26 не находился** (самая популярная игра). PS Store отдаёт «EA SPORTS FC™ 26» первой строкой, но матчер режет короткий запрос: «fc 26» (2 слова) против 8 слов имени бандла → покрытие 25% < 40%, буст не срабатывает, `not_found`. Фикс: `LATIN_ALIASES` в `server.js` (`fc 26/fc26/fc 25/fc25 → ea sports fc 25/26`), применяется к нелатинским... к латинским запросам через `aliasLatinQuery`, зеркалит кириллические `фк`/`фифа`. Коммит `f50e2b8`. Проверено: FC 26/FC26/fc 25 → `found` с обложками.

**Что решили:**
- Обложки PS Store, не IGDB и не прокси — точность по изданиям важнее нейтральности URL. IGDB оставлен фоллбэком (graceful: PS → IGDB → 🎮).
- Per-edition обложки бесплатны (уже в данных), но **кэш 6ч маскирует** правки парсера на популярных играх — для проверки брать свежую игру или `/test/search` (мимо кэша).
- `LATIN_ALIASES` обновлять по годам вручную (как кириллические аналоги) — приемлемо, т.к. FC/FIFA единственная франшиза, где аббревиатура критична.

**Новые баги:** не обнаружено (все найденные — исправлены в этой сессии).

**Открытые вопросы:** нет.

**Что осталось:**
- 🔴 Визуальная проверка обложек на сайте (модалка изданий + ambiguous) — единственный незакрытый хвост.
- 💡 Детектор поломки парсера (TG-алерт при серии `not_found`)
- 💡 Негативный кэш `not_found`

### 2026-06-13 (сессия 21)

**Что сделано (watch-фича закрыта end-to-end + фикс обложек):**
- **Шаг 4/4 watch — обработчик `/start w_TOKEN`.** Обнаружено, что код уже был написан и закоммичен (`3639cdd`) в `Standalone/chat-bot/tg-bot.js` (`handleMessage`, ветка `text.startsWith('/start w_')`) — прошлая сессия искала файл, но он уже был. Задеплоен на VPS (`~/poigraem-chatbot/`, `curl -L` + `pm2 restart poigraem-chatbot-tg`).
- **Баг Node 20 в tg-bot.js.** При старте `createClient` падал: «Node.js 20 detected without native WebSocket support». В `tg-bot.js` не было `import ws from 'ws'` + `{ realtime: { transport: ws } }` (соседние `server.js`/`logger.js` это имеют). Исправлено, коммит `afd5437`, задеплоено. После фикса в логе «TG-бот запущен (polling)».
- **Баг GRANT на `price_watchlist`.** `/init` падал с `permission denied for table price_watchlist`. Причина: таблицу создавали через SQL Editor → Supabase не выдал привилегии роли `anon` (в отличие от таблиц из Table Editor). Денис выполнил в Supabase: `GRANT SELECT,INSERT,UPDATE ON public.price_watchlist TO anon, authenticated, service_role;` + `GRANT USAGE,SELECT ON SEQUENCE public.price_watchlist_id_seq TO …`. После этого подписка заработала.
- **Боевой прогон пройден:** кнопка «Следить» → бот открылся → Start → бот ответил «Запомнил! Слежу за …». Цепочка подтверждена.
- **Баг пустых обложок.** В результате `found` с несколькими изданиями обложка не показывалась. Причина: `stripEditionSuffix` не снимал платформу **в скобках** `(PS5)` → «Ultimate Edition» оставалась в середине строки, в IGDB уходило полное имя со словами `ultimate/edition/ps5`, бэкенд `/igdb/cover` требует все слова в названии → `cover:null`, фоллбэк не срабатывал (`clean===base`). Подтверждено curl'ом: чистое имя → обложка, грязное → `{"cover":null}`. Исправлено: добавлен паттерн на скобки + чистка в цикле до стабилизации. Коммит `e170c3c`, задеплоено, обложка появилась.

**Что решили:**
- Деплой стандартно через `curl -L -o файл.new` → `grep`-проверка → `mv` → `pm2 restart`. Рейт-лимит price-lookup (in-memory) обнуляется рестартом — удобно для тестов.
- Per-edition обложки через отдельные IGDB-запросы делать **не** будем: издания — одна игра, IGDB вернёт ту же картинку N раз, только медленнее.

**Новые баги:** не обнаружено (все найденные — исправлены в этой сессии).

**Открытые вопросы:**
- 🔴 **Обложки изданий** (зафиксировано в «Текущем фокусе»). Денис заметил: у части игр у разных изданий разные обложки. IGDB этого не даёт (1 обложка/игру). Альтернатива — картинки PS Store (уже в распарсенных данных, 0 лишних запросов), но их URL (`image.api.playstation.com`) раскрывает источник, что против правила «серой зоны» брифинга. Решение за Денисом: точность (PS Store) vs нейтральность (IGDB).

**Что осталось:**
- 🔴 Решить и реализовать вопрос обложек изданий
- 💡 Детектор поломки парсера (TG-алерт при серии `not_found`)
- 💡 Негативный кэш `not_found`

### 2026-06-12 (сессия 20)

**Что сделано (деплой watch-фичи, 3 из 4 шагов):**
- **Supabase:** создана таблица `price_watchlist` (SQL из «Текущего фокуса»). На предупреждение Supabase о RLS выбрано «Run without RLS» — осознанно: `/init` пишет в таблицу анонимным ключом (`server.js:681`, клиент `supabase`), как и соседние `price_lookup_cache`/`price_lookup_requests`; включение RLS без политик сломало бы INSERT.
- **VPS `.env`:** в `~/price-lookup/.env` дописаны `CHATBOT_TG_TOKEN=8760595758:...` и `CHATBOT_BOT_NAME=Poigraem_Game_Helper_bot`. Значения взяты из чатбота: токен из `/home/ubuntu/poigraem-chatbot/.env`, имя бота получено через Telegram `getMe`.
- **Деплой кода:** `server.js` (775 строк, 3 совпадения `price-watch`) и `public/index.html` (483 строки, кнопка «Следить» на месте) скачаны через `curl -L -o ...new` из `raw.githubusercontent.com/.../main/Standalone/price-lookup/...`, проверены, заменены.
- **Рестарт:** `pm2 restart price-lookup --update-env` → `online`, логи чистые, `/price-lookup/api/config` отдаёт `"botName":"Poigraem_Game_Helper_bot"`.

**Что решили:**
- Таблицы price-lookup пока живут без RLS (анонимная запись) — осознанный текущий режим проекта. Перед публичным запуском навесить RLS + политики разом и перевести записи на service-ключ (та же тема, что красное «ротировать anon-ключи» в брифинге магазина).
- Деплой каждого файла — через временный `.new` + `head`/`wc`/`grep`-проверка перед `mv`, чтобы не затереть рабочий файл «обрезком» от редиректа (урок сессии 17, флаг `-L` обязателен).

**Новые баги:** не обнаружено.

**Открытые вопросы:**
- В чатботе TG-приём идёт отдельным процессом `poigraem-chatbot-tg` (id 6) — правку `/start w_TOKEN` нужно вносить туда, где этот процесс ведёт polling. Конкретный файл в `Standalone/chat-bot/` ещё не подтверждён (сессия прервана на поиске).

**Что осталось:**
- 🔴 Chatbot: обработка `/start w_TOKEN` (см. п.1 «Текущего фокуса») + деплой
- 🟡 Визуальная проверка всей цепочки кнопка → бот → Supabase → уведомление

### 2026-06-12 (сессия 19)

**Что сделано:**
- `server.js` — `POST /api/price-watch/init`: создаёт pending-строку в `price_watchlist` с 16-символьным токеном, возвращает `{token}`
- `server.js` — `GET /api/config`: добавлено поле `botName` (`CHATBOT_BOT_NAME` из env)
- `server.js` — `sendChatbotMessage(chatId, text)`: шлёт сообщение пользователю через `CHATBOT_TG_TOKEN`
- `server.js` — `checkWatchlist()` + `setInterval(6h)`: проходит по активным подпискам, вызывает `lookupPrice`, при падении ≥15% — уведомляет пользователя и деактивирует запись
- `server.js` — rate limit добавлен на `/api/price-watch`
- `public/index.html` — кнопка «👀 Следить за скидкой» под результатом `found` (одно и несколько изданий)
- `public/index.html` — `watchBtn()` + `watchGame()`: вызывает `/init`, открывает `t.me/BOT?start=w_TOKEN` (deep link), показывает «Открой бот и нажми Start»
- `public/index.html` — CSS `.btn-watch`
- Написана инструкция для агента chatbot: что добавить в `tg-bot.js` для обработки `/start w_TOKEN`
- Коммит `9214003`, запушено на GitHub

**Что решили:**
- Вариант Б (через chatbot): пользователь жмёт Start → chatbot сохраняет `chat_id` в `price_watchlist` → price-lookup проверяет цены и шлёт уведомление через `CHATBOT_TG_TOKEN`
- Кнопка скрыта если `botName` не задан в env — safe by default
- Порог уведомления: 15% (переопределяется через `WATCH_PRICE_DROP_THRESHOLD` в env)

**Новые баги:** не обнаружено.

**Открытые вопросы:** нет.

**Что осталось:**
- 🔴 Создать таблицу `price_watchlist` в Supabase (SQL в разделе «Текущий фокус»)
- 🔴 Добавить `CHATBOT_TG_TOKEN` и `CHATBOT_BOT_NAME` в `.env` price-lookup на VPS
- 🔴 Задеплоить `server.js` + `public/index.html` на VPS (`curl -L -O` + `pm2 restart price-lookup`)
- 🔴 Chatbot: добавить обработку `/start w_TOKEN` в `tg-bot.js` + задеплоить (по инструкции из этой сессии)
- 🟡 Визуальная проверка всей цепочки: кнопка → бот → Start → запись в Supabase → уведомление при падении цены

### 2026-06-12 (сессия 18)

**Что сделано:**
- `parsers.js` — `\bx\b` убран из конвертации римских цифр (предотвращает "X-Men" → "10 men")
- `parsers.js` — `isEditionOfSameGame`: добавлен кейс «запрос ≥3 слов → подзаголовок после запроса считается той же игрой» ("The Witcher 3 Wild Hunt", "God of War Ragnarok")
- `server.js` — `editions`-список строится с TR-приоритетом (IN-вариант вытесняет TR только при −20%), согласованно с логикой winner
- `server.js` — `getSettings` catch: при первом падении на дефолты отправляется `sendTgAlert` (rate=2.8 дефолт завышает цены на ~50%)
- `server.js` — `store_snapshot` запрос добавлен `.order('created_at', {ascending:false})` — берётся последняя строка
- `server.js` — оба admin-эндпоинта принимают токен через заголовок `X-Admin-Token` (query param тоже работает для обратной совместимости)
- `server.js` — `ip_hash` использует соль из `IP_HASH_SALT` env — брутфорс IP невозможен без знания соли
- `CLAUDE.md` — исправлена формула (мультипликативная → аддитивная), обновлён статус walking skeleton, убран устаревший `CACHE_TTL_HOURS`
- VPS: все файлы задеплоены через `curl -L -O`, `IP_HASH_SALT=DomMasiiMelisu2025` добавлен в `.env`, `pm2 restart price-lookup`
- Сессия 7 перенесена в `SESSIONS-ARCHIVE.md`

**Что решили:**
- `x` из римских убран, `v` оставлен — оба нормализуются одинаково, поэтому матч не ломается, но риск коллизий снижен
- Правильный путь для деплоя: `Standalone/price-lookup/` (заглавная S), записано в memory
- Все 22 актуальных пункта аудита Fable закрыты

**Новые баги:** не обнаружено.

**Открытые вопросы:** нет.

**Что осталось:**
- Визуальная проверка: «The Witcher 3», «Cyberpunk 2077» после фиксов
- 💡 Идеи из аудита: детектор поломки парсера, негативный кэш, кэш обложек

---

### 2026-06-11 (сессия 17)

**Что сделано:**
- `server.js` + `parsers.js` — **группа 2 из аудита Fable, коммит `b1851c0`**, 4 фикса:
  1. `limiter` добавлен на `/api/price-request` и `/api/suggest` (раньше только `/api/price-lookup` был защищён)
  2. `escHtml()` — новая утилита экранирует `&`, `<`, `>`. `gameName` и `contact` в `sendTgAlert` теперь экранированы
  3. `.limit(2000)` на demand-запрос в `/api/admin/logs` (раньше без лимита — мог вытащить всю историю)
  4. `₺` добавлен в `parsePriceText` — теперь понимает `₺1.145,00` наравне с `1.145,00 TL`

**Что решили:**
- Порядок деплоя: сначала исправить `curl` (нужен флаг `-L`), потом проверить файлы, потом pm2 restart.

**Новые баги:**
- `curl -O` без флага `-L` не следует редиректам GitHub raw URL → скачал 14 байт вместо файла. VPS содержит старые/пустые `server.js` и `parsers.js`. Нужно: `curl -L -O <url>` + `head -3 server.js` для проверки.

**Открытые вопросы:** нет.

**Что осталось:**
- Деплой на VPS: `curl -L -O` для обоих файлов + `pm2 restart price-lookup`
- Группа 3: editions vs winner, стоп-список, isEditionOfSameGame
- Проверить Cyberpunk и Alien визуально

### 2026-06-11 (сессия 16)

**Что сделано:**
- Аудит рекомендаций Fable (23 пункта): 22 актуальны, 1 устаревший (п.10 — `admin.html` demand уже реализован в текущем коде).
- `public/index.html` + `server.js` — **группа 1, коммит `b44cb50`**, 4 фикса:
  1. `fetch('/api/config')` → `fetch('/price-lookup/api/config')` — прямая потеря заказов (кнопка вела на хардкод `poigraem_shop`)
  2. `setCached` только если `result.length > 0` — пустые результаты (капча/сбой Sony) не кэшируются на 6 часов
  3. Guard `if (qBtn.disabled) return` в `doSearch()` — Enter не дублировал запрос во время загрузки
  4. `t.to === null` → `t.to == null` в `convertToRub` — ловит и `undefined` в тирах наценки

**Что решили:**
- Порядок исправления: группа 1 (однострочные) → группа 2 (небольшие) → группа 3 (логика).

**Новые баги:** не обнаружено.

**Открытые вопросы:**
- VPS деплой группы 1 ещё не выполнен — Денис делает вручную через `curl` + `pm2 restart`.

**Что осталось:**
- Задеплоить на VPS (server.js + public/index.html, pm2 restart price-lookup)
- Группа 2: rate limit, TG-экранирование, demand limit, ₺ парсер
- Группа 3: editions vs winner, стоп-список римских, isEditionOfSameGame для подзаголовков

### 2026-06-05 (сессия 15)

**Что сделано:**
- `parsers.js` — **`isEditionOfSameGame(queryNorm, nameNorm)`**: новая функция определяет, является ли кандидат изданием той же игры (остаток после query состоит из цифр + edition-слов) или другой игрой. Используется в `matchGame` auto-ветке: если все совпадения — одна игра → `found` (список изданий), иначе → `ambiguous` («Вы имели в виду?»).
- `parsers.js` — **EDITION_SUFFIX_WORDS** дополнен платформенными суффиксами: `ps`, `xbox`, `pc`, `cross`, `gen`, `next`, `native`, `version` — чтобы `(PS5)` в названии (→ `ps 5` после нормализации) не ломало `isEditionOfSameGame`.
- `parsers.js` — **GAME_CLASSIFICATIONS** расширен: добавлены `COMPLETE_GAME`, `DIGITAL_EDITION`, `DIGITAL_EXCLUSIVE_GAME`, `PS5_NATIVE_GAME`, `CROSS_GEN_BUNDLE` — likely причина фильтрации Ultimate Edition.
- `server.js` — **cross-region all-candidates**: `addRegionOptions` собирает `best + alternatives` из обоих регионов в `allOptions`. Побеждает абсолютно дешевейшее издание (не только `best` каждого региона).
- `server.js` — **`editions` вместо `alts`** в ответе: все варианты (включая выбранное), дедупликация по `normalizeQuery`, сортировка по цене, топ-3.
- `server.js` — кэш TTL: 24ч → 6ч.
- `public/index.html` — **новый UI `found`**: если `editions.length > 1` — показывает список изданий (каждое: название + цена + кнопка «🛒 В корзину»), cheapest подсвечен синим. Если одно издание — прежний вид.
- `public/index.html` — **большая модалка** (слайд снизу на мобиле, scale-in на десктопе) вместо маленького inline-блока.

**Что решили:**
- Не автовыбирать дешевейшее тихо — показывать клиенту список изданий и дать выбрать. Это и честнее, и вероятно лучше конвертирует.
- Разные игры (Alien vs Alien Returns) → `ambiguous`; издания одной игры (Cyberpunk Ultimate vs Standard) → `found` с выбором.
- PM2-имя процесса: `price-lookup` (id=4), НЕ `poigraem-price-lookup`.

**Новые баги:**
- `poigraem-editorial` ↺=40 за 5 часов — причина не исследована. Нужны логи.

**Открытые вопросы:**
- Cyberpunk Ultimate так и не проверен визуально после деплоя — Denis должен протестировать.
- Конкретная classification Ultimate Edition неизвестна — может потребоваться ещё расширить список.

**Что осталось:**
- Проверить Cyberpunk и Alien визуально на `api.poigraem.shop/price-lookup/`
- Разобраться с рестартами `poigraem-editorial`: `pm2 logs poigraem-editorial --lines 30`

### 2026-06-04 (сессия 14)

**Что сделано:**
- `parsers.js` + VPS — **фикс матчинга изданий**: разделены `isPrefix` (имя начинается с запроса → буст sim=1.0 без ограничений) и `isWordInMiddle` (запрос в середине → буст только при покрытии ≥40%). До фикса «cyberpunk» не буcтил «Cyberpunk 2077 Ultimate Edition» (1/4=0.25 < 0.4), и дешёвое Ultimate на скидке проигрывало обычному изданию.
- `price-lookup/public/index.html` + VPS — **вариант А воронки**: в результатах поиска добавлена синяя кнопка «🛒 В корзину магазина» → редирект на `poigraem.shop/?order=Name&price=X`.
- `js/client.js` — **вариант А на стороне магазина**: `checkOrderParam()` читает `?order=` + `?price=` при загрузке, добавляет `{type:'custom'}` в корзину, показывает тост, очищает URL через `history.replaceState`. Флаг `_orderParamChecked` — срабатывает только один раз.
- `index.html` + `styles.css` + `js/client.js` — **вариант Б воронки**: плашка «Не нашли игру?» теперь раскрывается в inline-виджет прямо в магазине. Поле ввода + fetch к price-lookup API + результат с обложкой и кнопкой «В корзину». При добавлении — виджет схлопывается, появляется корзина.

**Что решили:**
- custom-товары (`type:'custom'`) не участвуют в bundle-скидке — попадают в `otherTotal`. Корректно.
- `checkOrderParam` вызывается внутри `renderClient()` — гарантированно после того как магазин отрисован.
- Воронка «купить» закрыта полностью: два пути (inline-виджет из магазина и редирект из price-lookup).

**Новые баги:** не обнаружено.

**Открытые вопросы:**
- Визуальная проверка обоих вариантов не проведена.
- `poigraem-editorial` ↺=29 за ~2 часа — очень частые падения, причина не исследована.

**Что осталось:**
- Проверить визуально оба варианта воронки
- Разобраться с рестартами `poigraem-editorial` и `poigraem-chatbot`: `pm2 logs poigraem-editorial --lines 50`

### 2026-06-04 (сессия 13)

**Что сделано:**
- `parsers.js` — фикс матчинга изданий: `isPrefix` (имя начинается с запроса) теперь бустит до `sim=1.0` без проверки покрытия. Покрытие `qWords/nWords >= 0.4` применяется только к `isWordInMiddle`. Раньше "cyberpunk" не буcтил "Cyberpunk 2077 Ultimate Edition" (1/4=0.25 < 0.4), и дешёвое Ultimate на скидке не попадало в auto-корзину. Задеплоено на VPS.
- `index.html` + `styles.css` + `js/client.js` — **вариант Б воронки «купить»**: плашка «Не нашли игру?» теперь раскрывается в inline-виджет. Поле ввода + запрос к price-lookup API + результат (обложка, цена, кнопка «В корзину»). Кнопка добавляет `{type:'custom'}` в корзину магазина и схлопывает виджет. `ambiguous` → кнопки-кандидаты. `not_found` → ссылка в Telegram. Задеплоено.

**Что решили:**
- custom-товары в корзине: `type='custom'`, не участвуют в bundle-скидке (попадают в `otherTotal`).
- Исправление матчинга: разделить «начинается с запроса» (всегда буст) и «запрос встречается в середине» (буст только при покрытии ≥40%).

**Новые баги:** не обнаружено.

**Открытые вопросы:**
- Визуальная проверка inline-виджета не проведена — Denis ещё не смотрел на сайте.

**Что осталось:**
- Проверить виджет визуально на `poigraem.shop`
- Вариант А: `poigraem.shop/?order=Name&price=X` + кнопка редиректа в price-lookup
- Рестарты сервисов: `poigraem-editorial` ↺=17, `poigraem-chatbot` ↺=12

### 2026-06-04 (сессия 12)

**Что сделано:**
- `server.js` — кэш TTL: 7 дней → 1 день.
- `parsers.js` — фикс матчинга: добавлено условие покрытия `qWords / nWords >= 0.4` перед бустом `sim = 1.0`. Раньше "cyberpunk" (1 слово) буcтил "Jigsaw Abundance cyberpunk bundle" (4 слова) наравне с "Cyberpunk 2077", после чего выбирался дешевейший — Jigsaw.
- `server.js` — при `ambiguous` ответе подтягиваются `coverUrl` для каждого кандидата параллельно (timeout 2s).
- `public/index.html` — UI `ambiguous`: вместо текстовых кнопок — карточки с обложкой (`aspect-ratio:3/4`) и названием, заголовок «Вы имели в виду?». При отсутствии обложки — иконка 🎮.

**Что решили:**
- Порог покрытия 40% — безопасный: "god of war" (3) / "God of War Ragnarok" (4) = 75% ✅; "cyberpunk 2077" (2) / "Cyberpunk 2077 Ultimate Edition" (4) = 50% ✅.
- Кэш 1 день вместо 7 — цены в PS Store могут меняться чаще.

**Новые баги:**
- `poigraem-editorial` ↺=5, `poigraem-chatbot` ↺=4 — частые рестарты, причина не исследована.

**Открытые вопросы:**
- Воронка «купить»: архитектурное решение не принято.

**Что осталось:**
- Проверить логи `poigraem-editorial` и `poigraem-chatbot`
- Воронка «купить»

### 2026-06-04 (сессия 11)

**Что сделано:**
- Выяснили что VPS уже был актуален — сессия 9 задеплоилась до случайного закрытия терминала. Лог сессии 9 был неточен.
- `public/index.html` — атрибуция чатбота: читает `?ref=chatbot&game=Y` из URL при загрузке; если `game` — подставляет в поле и автозапускает поиск; `source: _source` передаётся в каждом запросе к `/api/price-lookup`.
- `server.js` — `/test/search` закрыт токеном (`ADMIN_TOKEN`): без токена → 401.
- `server.js` — CORS: `*` заменён на whitelist `['https://poigraem.shop', 'https://api.poigraem.shop']`.
- `server.js` — IGDB fallback для кириллицы: если словарь `CYRILLIC_TRANSLATE` не нашёл перевод, делается запрос в `IGDB_SUGGEST_ENDPOINT`, берётся первый результат. Если и IGDB ничего — `trustTop` как раньше.

**Что решили:**
- Словарь кириллицы остаётся как быстрый первичный путь; IGDB — запасной для неизвестных запросов.
- Баннер на `poigraem.shop` открывает price-lookup в новой вкладке (`target="_blank"`) → это другой домен, корзина `poigraem.shop` напрямую недоступна. Воронка «купить» требует архитектурного решения.

**Новые баги:** не обнаружено.

**Открытые вопросы:**
- Воронка «купить»: как именно реализовать — кастомный товар через URL-параметр или встроить price-lookup inline в основной сайт.

**Что осталось:**
- Воронка «купить» — решить архитектуру и реализовать

### 2026-06-03 (сессия 10 — разбор + планирование)

**Что сделано:**
- Восстановлен контекст потерянной сессии по git log — всё закоммичено, ничего не потеряно.
- Проведён анализ текущего состояния сервиса: что уже закрыто, где дыры, что влияет на деньги.

**Что решили (приоритеты):**
1. 🔴 Деплой — price-lookup + основной backend (новый `/igdb/suggest` эндпоинт) не задеплоены
2. Атрибуция чатбота — `index.html` не читает `?ref=chatbot&game=Y` из URL → всё логируется как `direct`; чинится быстро
3. Воронка «купить» — кнопка ведёт в Telegram, нет добавления в корзину; главная потеря конверсии
4. Обложки не кэшируются рядом с ценой — каждый раз бьём в IGDB заново
5. Кириллица через IGDB-автокомплит вместо ручного словаря — автокомплит уже есть
6. Мобильный Telegram: `https://t.me/` вместо `tg://` как в основном магазине
7. `/test/search` открыт в проде — закрыть или убрать
8. CORS `*` — сузить до своего домена

**Новые баги:** не обнаружено.

**Открытые вопросы:** нет.

**Что осталось:**
- Деплой на VPS (price-lookup + backend)
- Атрибуция чатбота (быстрый фикс)
- Воронка: кнопка «купить» → корзина магазина

### 2026-06-02 (сессия 9)

**Что сделано:**
- `server.js` — `cleanGameName()` расширен: убирает «кривые» апострофы `'` `'` (Marvel's, Assassin's → IGDB не находил обложку)
- `server.js` — stale cache fallback: если Sony недоступна/вернула ошибку, отдаём старый кэш без TTL вместо пустого ответа
- `server.js` + `public/admin.html` — TG-уведомления при заявке: `sendTgAlert()` шлёт в чат при `POST /api/price-request`. Аналитика спроса в admin: таблица незакрытых `not_found` запросов сгруппированных по частоте
- `server.js` — `stripEditionSuffix()` расширен: срезает платформенные суффиксы `PS4/PS5/Xbox One/Xbox Series` в конце названия перед IGDB-запросом
- `server.js` + `public/index.html` — IGDB autocomplete при вводе: дебаунс 350ms → `/api/suggest` → `/igdb/suggest` на основном бэкенде; клавиатурная навигация ↑↓, выбор → сразу запускает поиск
- `backend/server.js` (основной) — новый эндпоинт `GET /igdb/suggest?q=...`: возвращает массив названий игр из IGDB по частичному совпадению
- `index.html` (основной магазин) — sticky-баннер «Не нашли игру?» в низу главной страницы

**Что решили:**
- Stale cache — правильная деградация: лучше показать цену 8-дневной давности, чем ошибку
- Автокомплит — проксируем через свой `/api/suggest` → на основной backend, не напрямую в IGDB (токены на клиенте не светим)

**Новые баги:**
- `index.html` не читает `?ref=chatbot&game=Y` из URL — атрибуция чатбота не работает. Всё логируется как `direct`.

**Открытые вопросы:**
- Всё из этой сессии не задеплоено на VPS — нужен curl + pm2 restart для обоих процессов.

**Что осталось:**
- Деплой на VPS
- Атрибуция чатбота
- Воронка: кнопка «купить» → корзина магазина

### 2026-05-31 (сессия 8)

**Что сделано:**
- `public/index.html` + `server.js` — добавлены обложки игр из IGDB: при `status: found` сервис запрашивает `api.poigraem.shop/igdb/cover`, возвращает `coverUrl` в ответе. В UI — обложка слева от цены (flex-layout), `onerror` скрывает если не загрузилась.
- `backend/server.js` (основной) — улучшен IGDB-матчинг: `limit 5→10`, добавлены `legends/origins/plus/reloaded` в NOISE, умный fallback по пересечению слов, SPINOFF-проверка (если лучший матч — спин-офф с score < 80, возвращается `null`), NFD-нормализация в `norm()` (`Yōtei→Yotei`).
- `server.js` (price-lookup) — функция `cleanGameName()`: перед запросом в IGDB убирает диакритику и `™®©`. Это был корень проблемы — PS Store возвращает `Ghost of Yōtei™`, IGDB по такому матчил спин-офф.
- `server.js` — `fetchIgdbCover()` переписан: сначала пробует базовое название (без суффикса издания), затем полное чистое как фоллбэк.

**Что решили:**
- Правильное место чистки названий — price-lookup перед отправкой в IGDB, не бэкенд.
- Базовое название без "Deluxe Edition" матчится надёжнее — IGDB требует наличия всех слов запроса в названии.
- Лучше `null` (нет обложки), чем обложка другой игры.

**Новые баги:** не обнаружено.

**Открытые вопросы:**
- Финальный тест Ghost of Yotei после деплоя не был подтверждён (сессия завершилась сразу после рестарта PM2).

**Что осталось:**
- Проверить обложку Ghost of Yotei визуально
- Встроить виджет «Не нашли игру?» на основной сайт магазина
- Проверить кнопку «📊 Поиски» в основной админке

---

### 2026-05-31 (сессия 7)

**Что сделано:**
- Supabase: создана таблица `price_lookup_log`, добавлены явные `GRANT SELECT/INSERT ON price_lookup_log TO anon` и `GRANT INSERT ON price_lookup_requests TO anon` (новая политика Supabase с 30.05.2026)
- VPS: директория `~/price-lookup/`, файлы загружены через curl с GitHub, `.env` создан через nano+sed
- PM2 запущен (`price-lookup`, id=7), `pm2 save`
- nginx: добавлен блок `location /price-lookup/ { proxy_pass http://127.0.0.1:3001/; }` через nano
- Три фикса в коде: (1) `import ws from 'ws'` + `SUPABASE_OPTS` для Node.js 20, (2) `app.set('trust proxy', 1)` для nginx X-Forwarded-For, (3) URL в `public/index.html` с `/api/...` на `/price-lookup/api/...`
- `js/admin-core.js` основного магазина: URL price-lookup захардкожен, убран prompt

**Что решили:**
- `sb_secret_...` (новый формат Supabase) — НЕ аналог service_role. Нужен legacy JWT (`eyJ...`) из раздела Legacy
- Новая политика Supabase: новые таблицы требуют явного `GRANT` при создании
- URL в HTML для nginx-проксированных сервисов: полный путь `/price-lookup/api/...`, не `/api/...`

**Новые баги:** не обнаружено.

**Открытые вопросы:** нет.

---

### 2026-05-30 (сессия 6)

**Что сделано:**
- `server.js` — удалён дебаг-лог `console.log('[convert]...')` из `convertToRub`
- `server.js` — `convertToRub()` теперь возвращает `{ priceRub, markup, rateUsed }` вместо числа; обновлены все callers в `lookupPrice`
- `server.js` — добавлен `supabaseAdmin` (использует `SUPABASE_SERVICE_KEY`) для чтения закрытых таблиц в admin-эндпоинте
- `server.js` — добавлена `logSearch()`: пишет в `price_lookup_log` каждый поиск с полными деталями (запрос, игра, TR/IN цены, регион-победитель, курс, наценка, итог ₽, ip_hash, source)
- `server.js` — `lookupPrice()` возвращает `_details` во всех трёх ветках (found/ambiguous/not_found)
- `server.js` — обработчик `POST /api/price-lookup`: вычисляет `ipHash`, вызывает `logSearch` (fire-and-forget), срезает `_details` перед ответом клиенту
- `server.js` — добавлен `GET /api/admin/logs?token=X`: последние 100 поисков + 50 заявок из Supabase; защита по `ADMIN_TOKEN`
- `public/admin.html` — новый файл: тёмная тема, две таблицы (поиски + заявки), кнопка «Обновить»
- `index.html` (основной магазин) — кнопка «📊 Поиски» в тулбаре
- `js/admin-core.js` (основной магазин) — `openPriceLogs()`: первый клик → prompt для URL сервиса → `URL/admin?token=TOKEN`

**Что решили:**
- Admin-страница — отдельная вкладка, не модал; токен переиспользуется из основной админки
- `supabaseAdmin` с service role нужен для чтения `price_lookup_requests` (контакты клиентов)

---

### 2026-05-30 (сессия 5)

**Что сделано:**
- `public/index.html` — создана UI-страница для клиента: поле поиска → цена «от X ₽» с двумя кнопками (Telegram + ВКонтакте) → форма заявки при «не нашли» → подтверждение отправки
- `server.js` — критический баг в `getSettings()`: читал `data.settings.rate` вместо `data.rate` → все расчёты шли на дефолтах (rate=2.8, inrGift=1030 вместо реальных 1.8 и 900)
- `server.js` — `markupTiers` теперь тоже берётся из снапшота (`s.markupTiers`), с фоллбэком на таблицу `settings`
- `server.js` — логика выбора региона-победителя: Турция приоритет, Индия побеждает только если дешевле на 20%+
- `server.js` + `public/index.html` — два контакта менеджера: Telegram `@small_ben` и ВКонтакте `vk.com/club239030973`; `/api/config` отдаёт оба, HTML показывает две кнопки
- `.env` — обновлены `MANAGER_PRIMARY_VALUE=@small_ben`, добавлены `MANAGER_FALLBACK_TYPE=vk`, `MANAGER_FALLBACK_VALUE=https://vk.com/club239030973`

**Что решили:**
- Контакт менеджера не хардкодится в HTML — только через `/api/config`, меняется в `.env`
- Турция = основной регион. Индия = запасной, если экономия ≥20%
- Цены берутся из снапшота магазина — единый источник правды

---

### 2026-05-30 (сессия 4)

**Что сделано:**
- `server.js` — добавлен `import { createHash } from 'crypto'`
- `server.js` — добавлен `express.static('public')` (раздача UI-страницы)
- `server.js` — добавлен `GET /api/config` (возвращает `managerType` и `managerValue` из .env — несекретные данные для фронтенда)
- `server.js` — добавлен `POST /api/price-request`: сохраняет заявку в таблицу `price_lookup_requests`, хэширует IP через sha256, логирует в консоль
- `.env` + `.env.example` — добавлены `MANAGER_PRIMARY_TYPE=telegram` и `MANAGER_PRIMARY_VALUE=@poigraem_shop`

**Что решили:**
- Контакт менеджера не хардкодится в HTML, а читается через `/api/config` — можно менять без правки кода
- IP хранится только как sha256-хэш (Node.js built-in crypto, без доп. зависимостей)
- UI раздаётся из `public/` через `express.static` — один порт для API и страницы

---

### 2026-05-29 (сессия 3)

**Что сделано:**
- `server.js` + `package.json` — установлены `dotenv`, `@supabase/supabase-js`, `express-rate-limit`
- `server.js` — убраны `MOCK_RATES` и `MOCK_MARKUP`; добавлен `getSettings()` с кэшем 1 час: читает `markupTiers` из таблицы `settings`, курс TRY и цену INR-подарков из `store_snapshot`
- `server.js` — `convertToRub()` переписана в соответствии с логикой `pricing.js` магазина: абсолютная наценка из тиров, INR через подарки по 1000
- `server.js` — добавлен `express-rate-limit` (10 req/час на `/api/price-lookup`)
- `.env` и `.env.example` — созданы; Supabase URL и publishable key заполнены
- `server.js` — добавлен кэш цен в Supabase (`price_lookup_cache`, TTL 7 дней): `getCached()` / `setCached()`
- `server.js` — `CYRILLIC_TRANSLATE` расширен с 7 до 51 записи
- `.gitignore` — расширен: `*.log`, `*.err`, `recon/`, `.claude/`

**Что решили:**
- Plan B при бане Sony — скрейпить `psprices.com`
- `node_modules` не коммитить
- Кэш 7 дней; INR через подарочные карты по 1000 INR

---

### 2026-05-29 (сессия 2)

**Что сделано:**
- `server.js` — полный переворот: убран `cheerio`, `fetchProduct`, локальные дубли `parseTRPrice`/`parseINPrice`/`similarity`. Теперь всё через `parsers.js`.
- `server.js` — `searchInRegion(query, region)`: реальный запрос к PS Store + `parseSearchPage`. Результат — массив структурированных кандидатов.
- `server.js` — `lookupPrice(gameName, opts)`: `Promise.all` двух регионов, `matchGame` отдельно для каждого, выбор минимума по RUB через mock-курсы.
- `parsers.js` — в `normalizeQuery`: пробел между буквой и цифрой (`FC26 → fc 26`), словарик аббревиатур.
- `parsers.js` — в `matchGame`: опция `trustTop` — берёт первый eligible по позиции PS Store.
- `server.js` — словарик `CYRILLIC_TRANSLATE` и функция `translateQuery`.
- `package.json` — убран `cheerio`.

**Что решили:**
- `cheerio`/`fetchProduct` не нужны: все цены в `__NEXT_DATA__` → Apollo state.
- Для кириллицы — переводить на английский, не `trustTop`.
- `киберпанк` → `cyberpunk 2077` (с годом), иначе матчит JigSaw-пазлы.

---

### 2026-05-27 (сессия 1)

**Что сделано:**
- Создан walking skeleton проекта: 3 файла (`server.js`, `package.json`, `.gitignore`)
- Реализованы базовые парсеры цен: `parseTRPrice()` для турецкого формата `1.145,00 TL`, `parseINPrice()` для индийского формата `₹1,49,999.00` (с лакхами)
- Реализована функция схожести `similarity()` через Sørensen–Dice по биграммам
- Реализована функция `fetchHtml()` с правильными заголовками браузера (User-Agent, Accept-Language)
- Эндпоинты `/health`, `POST /api/price-lookup` (с заглушкой), `GET /test/product/:region/:conceptId` (для отладки)
- Захардкожены mock-курсы и mock-наценка для тестирования без Supabase
- Массив `TEST_QUERIES` с 7 тестовыми играми (включая кириллицу)

**Что решили:**
- Стартуем с 3 файлов вместо «правильной» структуры с папками — walking skeleton, разнесём по факту роста
- Standalone-сервис, отдельный репозиторий, отдельный порт 3001 — не модифицируем основной server.js магазина
- Курс и markupTiers читаем напрямую из общей Supabase магазина (вариант A)
- Парсинг через HTML + Cheerio (не GraphQL, не Selenium) — проще на старте
- ES Modules (`"type": "module"`), Node.js 18+ со встроенным fetch
- Защита механики ценообразования: клиенту НЕ показываем регион, локальную цену, формулу
- Формулировка для клиента: «ориентировочно от X ₽», самую дешёвую edition (включая Deluxe на акции — явно подсвечивать)

**Открытые вопросы:**
- Интеграция с корзиной магазина — custom_items в корзине vs теневые записи в каталоге (решим при разработке UI)
- Структура страницы поиска PS Store — Next.js, нужно проверить есть ли `__NEXT_DATA__` JSON или парсить HTML-карточки
- Точные пороги схожести для кириллицы — подберём эмпирически после реальных тестов

**Что осталось:**
- Реальный парсинг страницы товара (селекторы, извлечение всех edition, определение productType)
- Реальный парсинг страницы поиска через `__NEXT_DATA__` или HTML-карточки
- Фильтрация Add-on / Bundle / Theme от настоящих игр
- Подключение Supabase (чтение курса/markupTiers, запись кэша, запись заявок)
- Rate limiting (10/час, 50/день)
- UI: модалка с формой, кнопки «Хочу купить» / «Уточнить у менеджера»
- Связка с chatbot через UTM-метками
- Deploy на VPS PS.kz
