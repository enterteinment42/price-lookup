# Audits Archive — price-lookup

Закрытые аудиты, задачи и факты, вынесенные из CLAUDE.md, чтобы не раздувать оперативный контекст. Здесь лежит всё полностью отработанное (исправленное/подтверждённое закрытым) — целые аудиты и отдельные задачи. Открытое (хоть с одним живым пунктом) остаётся в CLAUDE.md целиком. Читается по команде `/session-audits-archive`.

⚠️ Аудит PL-1…PL-14 (сессия 25) на момент выноса — **полностью открыт**, остаётся в CLAUDE.md целиком, здесь его нет. Аудит PL-15…PL-22 (сессия 26) — открыт, кроме PL-19 (закрыт ниже). Аудит PL-23…PL-30 (сессия 29) — открыт, кроме PL-23 (закрыт ниже).

---

## Находки сессии 31 (найдены и закрыты в одной сессии)

Обе всплыли не в формальном аудите, а из репорта Дениса: функция магазина «дозаполнить цены» выдала для Marvel's Wolverine 1010 ₽. Claude воспроизвёл на живых страницах PS Store, что виноват парсер price-lookup.

### ✅ PL-32 — апгрейд-DLC выигрывал выбор минимальной цены, закрыт сессия 31, коммит `1b06454`

**Было:** по запросу «Marvel's Wolverine» PS Store отдаёт три товара: саму игру (FULL_GAME, 3449 TL), Deluxe (PREMIUM_EDITION, 3849 TL) и **«Marvel's Wolverine Digital Deluxe Edition Upgrade» (GAME_BUNDLE, 400 TL)**. Апгрейд — по сути DLC (бесполезен без базовой игры), но Sony помечает его `GAME_BUNDLE`, а этот тип в белом списке `GAME_CLASSIFICATIONS` (там же живут настоящие Ultimate-издания) → апгрейд проходил фильтр «только игры», `isEditionOfSameGame` роднил его с игрой, все три попадали в один ценовой пул, и `matchGame` выбором минимума (`server.js`) забирал апгрейд за 400 TL (≈1010 ₽). Две причины, обе необходимы: (1) `GAME_BUNDLE` в белом списке; (2) `normalizeQuery("Marvel's Wolverine")` из-за апострофа давал «marvel s wolverine» — **3 слова**, из-за чего в `isEditionOfSameGame` срабатывало правило «запрос ≥3 слов → остаток имени это подзаголовок той же игры» (слово `upgrade` в `EDITION_SUFFIX_WORDS` отсутствует и должно было отсечь, но правило «≥3 слов» отрабатывало раньше). Не разовый кейс: «Deluxe Upgrade» за копейки есть у Spider-Man 2, Hogwarts, Diablo IV и почти всех крупных релизов, там апостроф не нужен — имя и так длиннее 3 слов.

**Фикс (варианты А+Б, выбор Дениса; вариант «ужесточить правило ≥3 слов» отклонён как рискованный):**
- **А** — `UPGRADE_RE = /\bupgrade\b/` в `parsers.js`; в первичном фильтре `matchGame` (до развилки trustTop) кандидат со словом `upgrade` в имени отбрасывается, если сам запрос апгрейд не содержит (`queryWantsUpgrade`).
- **Б** — апострофы (`'’‘\`´`) в `normalizeQuery` удаляются **без пробела** (до общей чистки пунктуации): «Marvel's» → «marvels» (2 слова). Самостоятельный баг: «marvel s» портил и биграммное сходство всем тайтлам с апострофом (Assassin's, Tom Clancy's).

**Подтверждение закрытия:**
- Юнит-тесты: новая секция 2d (6/6, синтетические кандидаты Wolverine — best = сама игра не апгрейд, цена 3449 не 400, апгрейд не в alternatives, явный запрос апгрейда его не теряет), 2b/2c без регрессий, обновлён ассерт normalizeQuery (`assassins creed`).
- Регрессия на живых страницах: тот же набор (Wolverine, Spider-Man 2, AC Mirage, Hogwarts) прогнан через версию из HEAD и исправленную — изменился ровно Wolverine, остальные без изменений. Spider-Man 2 отдаёт Deluxe дешевле базовой — это честная скидка −45% на полное издание, а не подмена апгрейдом; `UPGRADE_RE` там корректно молчит.
- Живой деплой (VPS-агент): «Marvel's Wolverine» **1010 ₽ (апгрейд) → 7360 ₽ (сама игра)**, `neutralLabel` = «Marvel's Wolverine», тесты 6/6 на живом файле, error.log пуст. Перепрайс уже записанных 1010 ₽ Денис сделал вручную (фикс лечит только будущие дозаполнения).

### ✅ PL-33 — порог выбора региона TR/IN снижен 20% → 12%, закрыт сессия 31, коммит `394bd52`

**Было:** обе цены (TR и IN) конвертируются в рубли, дальше берётся **не** минимум — Турция приоритетный регион (обычно дешевле и стабильнее по механике гифт-карт), Индия побеждает, только если дешевле турецкой на ≥X%: `inRub <= trRub * factor`. Порог был зашит литералом `0.8` (≥20%) в **двух** местах `server.js` (выбор winner + дедупликация списка изданий). Класс «новых игр» вида **3449 TL / 4999 ₹** имеет разрыв ~13.7% — при пороге 20% Индия проигрывала, хотя реально дешевле, и клиент видел завышенную турецкую цену (Wolverine после фикса PL-32: TR 7360 ₽ vs IN 6350 ₽).

**Фикс:** порог опущен до 12% и вынесен в единую константу `TR_PRIORITY_FACTOR = 1 - 0.12`, применяется и в выборе winner, и в дедупликации изданий — раньше два независимых литерала `0.8` легко разъехались бы при правке. 12% выбран из-за запаса ~127 ₽ над маркерным классом (13% давал 53 ₽ — при движении курса игры прыгали бы между регионами). Правило целиком задокументировано в CLAUDE.md (раздел «Выбор региона» в «Архитектурных решениях») — раньше нигде не было, знаменатель приходилось выводить из кода.

**Разобранная тонкость:** формулы «Индия дешевле TR на X%» (знаменатель — TR) и «TR дороже IN на X%» (знаменатель — IN) эквивалентны как границы, но дают **разные числа** на одном разрыве (13.72% от TR = 15.91% от IN). Проект везде считает проценты от большей цены (скидки PS Store, `WATCH_PRICE_DROP_THRESHOLD`) — порог оставлен в этой метрике, знаменатель зафиксирован в коде и доке, чтобы правило не поехало молча при переписывании.

**Подтверждение закрытия (живой деплой, VPS-агент):**
- «Marvel's Wolverine» **7360 ₽ (TR) → 6350 ₽ (IN)**, в логе `[winner] TR=7360 IN=6350 → 6350`.
- Калибровка на реальных числах: `[getSettings] rate=1.76 inrGift=1050 rounding=10` из Supabase совпал с расчётным (3449×1.76+наценка=7360; 5×1050+наценка=6350).
- Регрессия: Cyberpunk (TR 2610 vs IN 2760), Spider-Man 2 (4610 vs 5160), Witcher 3 (310 vs 1400) — все остались в Турции (там дешевле), выбор корректен. error.log пуст.
- ⚠️ Открытый хвост: порог откалиброван под один класс игр — стоит последить по логам `[winner]`, какая доля каталога уехала в Индию в зоне 12–20%, не задело ли лишнего.

---

## Закрытые находки внутри аудита PL-23…PL-30 (сессия 29)

### ✅ PL-23 — закрыт, сессия 29 (код) + сессия 30 (живой прогон), коммиты `0b2ef15` + `eec6cc4`

**Было:** `parsers.js`, режим `trustTop` (кириллический запрос вне `CYRILLIC_TRANSLATE`, не переведённый IGDB): `alternatives = eligible.slice(1, 4)` — соседние результаты поиска Sony попадали в список «изданий» без проверки, что это действительно та же игра. `lookupPrice` сваливал `best` + `alternatives` обоих регионов в общий ценовой пул и мог выбрать минимум с чужим названием — клиенту показывалась посторонняя игра как «издание».

**Фикс:** `isEditionOfSameGame(queryNorm, nameNorm, strict)` получил параметр `strict` (отключает правило «запрос ≥3 слов → остаток это подзаголовок», которое при сравнении двух ПОЛНЫХ имён товаров ложно роднило сиквелы, напр. «god of war ragnarok» сходило бы за издание «god of war»). В trustTop-ветке `alternatives` теперь фильтруются двусторонней strict-проверкой относительно `best`. Юнит-тест 2b в `test-parsers.js` (синтетические кандидаты, не требует `recon/`).

**Подтверждение закрытия:**
- Юнит-тест 2b — 4/4 локально и на живом задеплоенном файле VPS (сессия 29).
- Живой trustTop-прогон (сессия 30, попутно с проверкой PL-31): запрос «резидент ивел 4» реально прошёл через ветку `trustTop` на проде и отработал корректно (см. PL-31 ниже) — единственное, чего не хватало для закрытия PL-23 («живой запрос через trustTop-ветку не прогонялся»), теперь выполнено.

⚠️ Сам фикс PL-23 (фильтр `alternatives` относительно `best`) не защищал сам `best` — эта дыра осталась и её отдельно закрыл PL-31 (см. ниже) в сессии 30.

### ✅ PL-31 — закрыт, сессия 30, коммит `74854ae`

Найден не в ходе формального аудита, а во время приёмки пакетов сессии 28: Денис попросил Claude выполнить визуальную/API-приёмку живого сайта (iPhone для клик-теста не было), и в процессе живого прогона кириллических запросов обнаружился баг.

**Было:** в режиме `trustTop` (`parsers.js`) `best = eligible[0]` брался **безусловно**, без всякой проверки релевантности запросу — `similarity()` (биграммное Dice-сходство) там бесполезен, т.к. кириллический запрос и латинское имя кандидата никогда не пересекаются по биграммам (это и была причина, по которой Dice сознательно пропускался в trustTop). Живой пример: запрос «резидент ивел 4» (Resident Evil 4, транслитерация нестандартная, не в словаре `CYRILLIC_TRANSLATE`, IGDB не перевёл) — сырой кириллический поиск PS Store вернул слабые/нерелевантные совпадения, и первое из них («4PGP», посторонний товар) уверенно отдавалось клиенту как «found» с ценой 1990 ₽. Фикс PL-23 (см. выше) защищал только `alternatives` относительно `best` — сам `best` не проверялся никогда, поэтому баг мог всплыть на любом кириллическом запросе с нестандартной транслитерацией или редкой игрой, не только на этом конкретном названии.

**Решение (вариант выбран Денисом из трёх предложенных):** новая `translitCyrillic()` — грубая фонетическая карта кириллица→латиница (а→a, ж→zh, щ→sch и т.д.). В trustTop-ветке кандидаты теперь **переранжируются** по `similarity(translit(query), normalizeQuery(name))`, а не берётся первый по позиции Sony:
- сходство ≥ `TRUST_CONF` (0.45) → `found` (уверенный матч);
- `TRUST_GREY` (0.28) ≤ сходство < `TRUST_CONF` → `ambiguous` (клиент выбирает сам через «вы имели в виду?», а не сервис угадывает);
- сходство < `TRUST_GREY` → `not_found` (честная заявка менеджеру лучше уверенно неправильной цены).

Фильтр изданий PL-23 вынесен в переиспользуемый хелпер `trustTopEditions()`.

**Подтверждение закрытия (сессия 30, VPS-агент):**
- Юнит-тест 2c (синтетические кандидаты, не требует `recon/`) — 5/5: best = «Resident Evil 4» (не «4PGP»), «4PGP» не в alternatives, Deluxe-издание корректно осталось в alternatives, чисто мусорный запрос → `not_found`, а не `found` наугад.
- Регрессия проверена: тест 2b (PL-23) по-прежнему 4/4.
- **Живой прогон до/после рестарта на VPS:** ДО рестарта (старый код) реальный запрос «резидент ивел 4» дважды подряд возвращал «4PGP» с ценой — баг воспроизведён на проде. ПОСЛЕ рестарта тот же сырой кандидат «4PGP» по-прежнему приходит от Sony, но rerank его больше не пропускает → честный `not_found` вместо чужой цены.
- Пороги `0.45`/`0.28` эвристические (транслитерация фонетическая, неточная — например «резидент» даёт «rezident» вместо «resident»), откалиброваны на здравом смысле и синтетических тестах, не на реальном трафике — стоит последить за первое время.

---

## Закрытые находки внутри аудита PL-15…PL-22 (сессия 26)

### ✅ PL-19 — закрыт, сессия 28, коммиты `0eafa89` (код) + Supabase GRANT (Денис)

**Было:** `POST /api/price-watch/init` писал `priceRub` из тела запроса напрямую в `price_at_subscribe_rub` без валидации диапазона/типа — можно было подписаться с искусственно завышенной ценой и получить гарантированное «подешевело» при первой проверке. Плюс: pending-записи (`active:false`, если клиент не нажал Start в боте) не удалялись автоматически — таблица `price_watchlist` копила мусор.

**Фикс:**
- `server.js` — валидация `priceRub` (`Number.isFinite`, `0 < x ≤ 100000`) перед записью, иначе 400.
- Новая `cleanupStaleWatches()` — удаляет строки `active:false AND tg_chat_id IS NULL AND notified_at IS NULL AND created_at < now()-7d`; вызывается в конце каждого прогона `checkWatchlist()`. История уже сработавших уведомлений (`notified_at` не пуст) не трогается.

**Скрытая зависимость, найденная при деплое:** `cleanupStaleWatches()` требовала `GRANT DELETE ON price_watchlist TO service_role` — таблица создана через SQL Editor без дефолтных грантов (тот же класс проблемы, что в сессии 21 для SELECT/INSERT/UPDATE, тогда DELETE просто забыли). Денис выполнил грант вручную в Supabase SQL Editor.

**Подтверждение закрытия (живой прогон, сессия 28):**
- Валидация: `priceRub: 999999999` → `400`, запись в БД не создана (проверено SELECT'ом — единственная тестовая строка «Test Game» в таблице имела цену 2500, не 999999999).
- Грант: после `pm2 restart price-lookup` в `error.log` нет свежих `permission denied` (единственная такая строка в файле — с меткой времени ДО гранта).
- Очистка реально работает: `out.log` после рестарта содержит `[watchlist] cleaned up 1 stale pending subscriptions` — функция нашла и удалила настоящую брошенную запись.

---

## Закрытые задачи

### ✅ #1 «Следить за ценой» — закрыто, сессии 19–21

Реализована watch-фича: кнопка «👀 Следить за скидкой» на результате поиска, таблица `price_watchlist`, эндпоинт `POST /api/price-watch/init` (pending-строка с 16-символьным токеном), `checkWatchlist()` + `setInterval(6ч)` с порогом падения цены 15% (`WATCH_PRICE_DROP_THRESHOLD`), уведомление через `CHATBOT_TG_TOKEN`, deep-link `t.me/BOT?start=w_<token>` и обработчик `/start w_TOKEN` в `Standalone/chat-bot/tg-bot.js`.

**Подтверждение закрытия (сессия 21):** боевой прогон пройден end-to-end — кнопка «Следить» → бот открылся → Start → бот ответил «Запомнил! Слежу за …». Цепочка кнопка → бот → Supabase → уведомление подтверждена.

⚠️ У самой фичи позже (сессия 26) найдены отдельные баги — **PL-16** (`window.open` после `await` глохнет на iOS Safari), **PL-17** (переключение изданий сбрасывает состояние кнопки), **PL-19** (нет валидации `priceRub` + не чистятся брошенные pending-подписки — ✅ закрыт сессией 28, см. выше), **PL-6** (checkWatchlist бомбит Sony без пауз — исправлен сессией 28, коммит `0eafa89`, визуальная приёмка не проведена). PL-16/PL-17 живут в CLAUDE.md (раздел аудита сессии 26), исправлены в коде сессией 28, тоже ждут визуальной приёмки. Здесь зафиксирован только сам факт «фича реализована и работает end-to-end», не эти баги.
